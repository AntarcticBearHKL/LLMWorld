class AnalyzeSeasonal
    季节性负荷分析（计划85，Jin 2021 季节驱动变异，0 LLM）。

    - season_of(date_dir) → 南半球季节（season_for_date）
    - evening_kwh(load_watts) → 晚峰 16-21 点 kWh
    - build_report(per_house) → 按季节分组（夏/秋/冬/春）：
      samples / mean_kwh / mean_peak_watts / mean_evening_kwh /
      mean_peak_hour
    - CLI：python analyze_seasonal.py <world> --scenario
    - 输出：outputs/<world>/analysis/seasonal_<scenario>.json

    调用链：server /api/worlds/<id>/seasonal/<scenario>
    → 前端"季节性对比"面板
