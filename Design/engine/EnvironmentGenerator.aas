class EnvironmentGenerator
    prompt 提示词模板加载器对象，默认为 Prompt()

    func generate_district(user_prompt):
        根据用户的原始文字描述（如“位于墨尔本的中产家庭”），生成该仿真地区的数据。
        1. 载入 `generate_step1_district` 模板。
        2. 调用大语言模型，并结合思维链（Thinking）得到生成内容。
        3. 反序列化模型返回的 JSON，提取包含城市、区县、经纬度地理坐标、经济水平、人文背景描述等数据结构的字典。

    func generate_household_distribution(district_info):
        根据地区特征，仿真生成该社区的家庭构成类型与比例分布。
        1. 载入 `generate_step2_household_distribution` 模板并渲染。
        2. 调用大模型，返回包含总家庭数、以及各个家庭类型（如单身、年轻夫妻、核心家庭）的户数和占比字典。

    func generate_household(district_info, household_type):
        为特定的家庭类型，具体生成其微观家庭成员、房间布局以及拥有的电器配置明细。
        1. 从 appliances 模块读取当前系统支持的所有电器类型名称及配置 schema。
        2. 加载并渲染 `generate_step3_household` 模板。
        3. 调用大语言模型（单次请求）生成完整的家庭资产与成员画像 JSON（成员角色作息特征、健康状况偏好、专属移动电器、各个房间的电器数量年限等）。

    func expand_setting(user_prompt):
        在地区初始化时，帮助用户自动扩写家庭背景描述，输出结构化的 JSON 环境属性。

    func get_weather_data(location, date):
        获取模拟世界当天的气象实况，与 WeatherAPI 对接。如无 API 密钥配置则自动生成 mock 气候。

    func get_holiday_data(location, year):
        获取指定地区和年份的公众节假日数据。

    func generate_home_structure(environment):
        根据住房面积和经济水平，为家庭设计合理的房间面积和电器品牌功率配置。

    func generate_members(environment, home):
        根据家庭大类，自动生成 2-5 名具体家庭成员的姓名、年龄、性别、作息表、温度偏好特征及随身专属电器。

    func _generate_mock_weather(location):
        根据地理位置，返回合理温度区间的 mock 历史天气字典。

    func _generate_mock_holidays(location, year):
        根据地理国家代码（如CN、AU、US），提供法定节假日的 mock 日历回退映射。


class DateHelper
    @静态方法
    func is_weekend(date):
        判断给定的日期是否是周六或周日。

    @静态方法
    func is_holiday(date, holidays_data):
        判断给定的日期是否处于公众假期数据集中。

    @静态方法
    func is_workday(date, holidays_data):
        如果不是周末且不是公众假期，则该日期为工作日。

    @静态方法
    func get_holiday_name(date, holidays_data):
        查询给定日期在节假日数据中的正式中文或英文名称。
