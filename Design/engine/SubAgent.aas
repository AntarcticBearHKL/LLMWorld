class SubAgent
    _lock 线程并发锁，用于保障 Token 统计与并发监控的多线程安全
    _total_prompt_cache_hit_tokens 累计大语言模型 Prompt 缓存命中的 Token 数量，默认为 0
    _total_prompt_cache_miss_tokens 累计大语言模型 Prompt 缓存未命中的 Token 数量，默认为 0
    _total_completion_tokens 累计大语言模型生成的输出 Token 数量，默认为 0
    _current_concurrent 当前同时在飞的请求数，默认为 0
    _peak_concurrent 实测并发峰值（用于验证"并发 ≤10"硬约束），默认为 0

    MAX_WORKERS 并发硬上限（取 config.MAX_WORKERS，固定为 10，禁止调大）
    MAX_RETRIES 网络/服务端错误自动重试次数（取 config.MAX_RETRIES，固定为 3）
    RETRY_BACKOFF_SECONDS 指数退避的基数秒数，默认为 2
    REQUEST_TIMEOUT_SECONDS 单次请求超时秒数，默认为 120
    _GLOBAL_SEMAPHORE 全局信号量（容量 MAX_WORKERS）：即使多模块（如多户并行）同时发起调用，
        同一时刻真正打向 API 的并发也不超过 10

    异常 LLMCallError：调用失败（重试后仍失败）时抛出，调用方必须显式处理，绝不静默返回错误字符串。

    @静态方法
    func _clean_json_response(text):
        自动清洗大模型应答文本中的 Markdown 代码块包裹标记（如 "```json ... ```"），提取出合法的纯 JSON 字符串。

    @静态方法
    func _update_tokens(cache_hit, cache_miss, completion):
        多线程安全地累加更新当前模型请求消耗的各类 Token 计数器。

    @静态方法
    func get_tokens:
        线程安全地获取当前全局累计的（未命中、命中、输出）Token 使用量元组。

    @静态方法
    func reset_tokens:
        线程安全地将全局 Token 计数器清零复位。

    @静态方法
    func get_concurrency_stats:
        线程安全地返回（当前并发数，历史峰值并发数）元组，峰值用于验证并发上限约束。

    @静态方法
    func call_deepseek(prompt, json_mode, thinking, api_key, model):
        使用 deepseek-v4-flash 模型发送同步阻塞的单次对话请求。
        1. 未配置 API 密钥时直接抛出 LLMCallError。
        2. 若启用 thinking 模式，在载荷中注入 thinking 与 reasoning_effort 参数启用思维链；非 thinking 的 json_mode 注入 json_object 响应格式。
        3. 在全局信号量保护下计数并发，发送请求并统计 Token 消耗，返回包含内容、推理过程与是否带思维链的字典。
        4. 网络错误、非 200 状态码、响应含 error 或无 choices 均抛出 LLMCallError。

    @静态方法
    func call_with_retry(prompt, json_mode, thinking, api_key, model):
        带指数退避重试的单次调用。
        1. 最多尝试 MAX_RETRIES（3）次。
        2. 失败时按 RETRY_BACKOFF_SECONDS * 2^次数 等待后重试。
        3. 全部重试仍失败则抛出 LLMCallError。

    @静态方法
    func parallel_call(prompts, json_mode, thinking, api_key, model):
        利用 ThreadPoolExecutor（max_workers=MAX_WORKERS）并发调用大模型处理批量 Prompt。
        1. 空列表直接返回空结果。
        2. 每个 prompt 提交一个任务，内部走 call_with_retry，结果按原始输入顺序组装。
        3. 任一任务失败则抛出携带 prompt 序号的 LLMCallError，调用方需显式处理。
        4. 并发受线程池与全局信号量双重约束（≤10），用于并行模拟多成员一日计划或批量电器决策。

    @静态方法
    func single_call(prompt, json_mode, thinking, api_key, model):
        执行单次大语言模型同步调用（自动带重试），返回包含生成内容和推理内容的字典。
