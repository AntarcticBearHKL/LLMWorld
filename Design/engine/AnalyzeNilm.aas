class AnalyzeNilm
    NILM 负荷分解基准（计划91，Azad 2023 综述指标，0 LLM）。

    - load_true_appliances(house_dir) → 电器真值
      （电器级 JSON：unique_id/name/power_watts/total_energy_kwh）
    - find_plateaus(load_watts, min_minutes=10, tolerance=0.05)
      → 聚合曲线功率稳定平台（±5% 波动内）
    - match_appliances(plateaus, appliances) → 功率指纹匹配（±10%）
      → 估计各电器 kWh（叠加时段归大功率电器=简单 NILM 局限）
    - build_report(load_watts, appliances) → 每电器真值 vs 估计：
      mae_kwh / relative_error；总 MAE / disaggregation_rate
    - CLI：python analyze_nilm.py <world> --scenario [--date]
    - 输出：outputs/<world>/analysis/nilm_<scenario>_<date>.json

    用途：评估 LLM 模拟负荷曲线的可分解性（IHD 个性化反馈
    与真实 NILM 研究的数据基础）。

    调用链：server /api/worlds/<id>/nilm/<scenario>/<date>
    → 前端"NILM 分解基准"面板
