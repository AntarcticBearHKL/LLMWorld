class Room
    name 房间名称，例如 "主卧"、"客厅"，默认为 空
    appliances 该房间内放置的公用电器对象列表，默认为 空列表

    func add_appliance(appliance):
        将指定的电器对象放置在此房间，并将其物理位置属性更新为当前房间的名称。

    func add_appliance_by_name(appliance_name):
        使用电器工厂根据名称创建对应的电器实例，自动将其物理物理位置设为当前房间，并添加至电器列表中。

    func get_appliance(unique_id):
        在房间拥有的电器中快速检索符合唯一ID的电器对象。

    func get_appliances_info:
        生成包含房间内所有电器的辅助描述信息列表，如电器名称与用电类型。

    func to_dict:
        将房间及其放置的全部电器明细信息（序列化后的电器属性）转换为字典。
