class AnalyzeForecast
    负荷预测与可预测性分析（计划101，Zharova 2022，sklearn）。

    - weekday_of(date_dir) → 星期（0-6）
    - evaluate_forecast(days, min_days=5) → 每户：
      RandomForestRegressor(100 树, seed=42) 预测次日 24h
      特征=[hour, weekday]；naive 基线=重复昨日；
      mae_kw / mape / naive_mae_kw / naive_mape / improvement_pct
      （正提升=负荷有规律可循=模拟真实感维度）
    - build_report(per_house) → per_house 数组（数据不足户跳过）
    - CLI：python analyze_forecast.py <world> --scenario
    - 输出：outputs/<world>/analysis/forecast_<scenario>.json

    调用链：server /api/worlds/<id>/forecast/<scenario>
    → 前端"负荷预测与可预测性"面板
