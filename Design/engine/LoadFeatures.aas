class LoadFeatures
    engine/load_features.py 负荷特征与聚类族（纯逻辑，0 LLM，numpy + sklearn）。

    形状特征：
    - hourly_means(profile_watts) → 24h 小时均值（不足 1440 抛 ValueError）
    - normalize_shape(hourly) → 除以总和（形状分布，去量级）
    - load_factor(hourly) = mean/peak；peak_to_mean(hourly) = peak/mean
    - peak_hour(hourly) / valley_hour(hourly)

    聚类（sklearn.cluster.KMeans，k-means++/n_init=10，§一.22 库实现）：
    - kmeans(features, k, seed=42, iters=300) → (labels, centers, wcss)
      样本数 < k 或 distinct 形状数 < k 抛 ValueError（防退化空簇）
    - elbow_scores(features, k_max=8) → [{k, wcss}]，遇退化提前 break
    - auto_k(features, k_max=8) → 肘部法（最大增益点）

    跨日变异性（Zhou 2016 / Jin 2021 / Dent 2014）：
    - hourly_cv_curve(daily_hourly) → 24 维逐小时跨日 CV（mean=0 安全）
    - variability_index(daily_hourly) → 逐小时 CV 均值（0=完全规律）
    - peak_hour_shift(daily_hourly) → 每日峰时样本标准差（小时）
    - daily_kwh_cv(per_day_kwh) → 日总用电变异系数

    峰值叠加（Thorvaldsen 2021 需量电价配套）：
    - peak_overlap_events(watts, threshold=8000) → 连续 ≥阈值分钟段事件
      [{start/end_minute, start/end_time, peak_watts, duration_minutes}]
    - peak_overlap_count / peak_overlap_minutes

    调用链：被 load_profile_cluster / analyze_variability /
    analyze_behavior_patterns / make_analysis_all / energy_calculator 复用
