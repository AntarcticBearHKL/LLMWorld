class CombineWorlds
    多世界联合聚合：每世界 ≤10 户约束下，合并多世界同场景同日曲线 → 更大人口（0 token）。

    func load_profile(world_id, scenario, date):
        读取某世界的聚合曲线，不存在返回 None。

    func combine(world_ids, scenario, date):
        1. 逐个世界加载曲线（缺失世界跳过并提示）。
        2. 1440 分钟曲线逐元素求和。
        3. per_house 合并，house_id 加世界前缀（pop02:house_0001）。
        4. 统计重算（总/户均/中位/峰值）。
        5. 返回联合曲线 dict。

    func main:
        --worlds pop02 pop03 --scenario baseline --date 2026-04-21
        → outputs/combined_<w1>+<w2>/population/<scenario>/<date>/population_profile_1440min.json
        （server 的虚拟世界机制自动识别该目录，前端可直接查看）

    典型结果：combined_pop02+pop03 = 22 户，总 319.21 kWh，峰值 58,450W@18:00
