class MakeAnalysisAll
    分析工具链一键化（计划46，0 LLM，编排器不新增算法）。

    - scan_scenarios(world_id) → {scenario: [dates]}（自动扫描 population/）
    - run_all(world_id, date_arg) → 每场景依次执行：
      load_profile_cluster（聚类，指定/最新日期）
      analyze_variability（变异性，多日）
      analyze_behavior_patterns（模式迁移，多日）
      analyze_population（归因）
      全场景后执行：
      analyze_groups --label-source awareness
      analyze_groups --label-source variability
      compare_policies --all（政策矩阵，无 baseline 时跳过不崩溃）
    - 复用各工具 build/report 函数（不走子进程），汇总打印
    - CLI：python make_analysis_all.py --world <id> --date [可选]

    输出目录统一：
    outputs/<world>/analysis/（clusters_*/variability_*/patterns_*/
    population_*/groups_*）+ comparison/policy_matrix.json
