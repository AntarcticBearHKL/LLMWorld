class NewsItem
    date 发布日期（YYYY-MM-DD），决定该新闻何时对 agent 可见
    time 当日可获取时刻（HH:MM，默认 07:00）
    title 新闻标题
    content 新闻正文
    source 来源（政府公告/新闻媒体/社交媒体/社区公告）
    news_type 类型（政策/经济/社会/环境）

class NewsBoard
    items 全部新闻列表（按日期+时间升序）
    events_file 上帝剧本文件路径（worlds/<world_id>/events.json）

    func load_events(path):
        从 JSON 剧本文件加载新闻（上帝静态剧本）。
        1. 读取文件中的 events 列表。
        2. 逐条构造 NewsItem 并加入 items。
        3. 按（日期, 时间）排序。

    func add_event(news_item):
        上帝运行时注入单条新闻（API 方式）。
        1. 加入 items。
        2. 重新排序。

    func add_inline_event(text):
        命令行格式注入："日期|标题|内容[|来源]"。
        1. 按 | 拆分字段。
        2. 不足 3 个字段则抛 ValueError（格式错误显式报错）。
        3. 构造 NewsItem（来源缺省"官方公告"）并加入。

    func available_on(date_str):
        返回某日可获取的新闻（发布日期 <= 当日）。
        1. 过滤 items 中 date <= date_str 的条目。

    func render_for_prompt(date_str):
        渲染成注入 prompt 的 markdown 章节。
        1. 无新闻返回空串（调用方需容忍空）。
        2. 有新闻输出"## 今日外界信息"标题与逐条列表。
        3. 结尾附"结合性格/职业分析"指令，让 LLM 做个性化解读。

    func to_json:
        导出全部新闻为 JSON 字符串。
