class World
    home 管理当前世界的家庭对象
    time 当前世界的环境时间，默认为 Time()
    history 存储每日模拟结果的历史记录列表，默认为 空列表
    current_planner 当前时间段对应的计划器对象，默认为 空
    current_executor 当前时间段对应的执行器对象，默认为 空
    world_id 当前世界的唯一标识符，默认为 空
    postcode 当前世界所在的邮政编码，默认为 空
    house_id 当前世界中的房屋标识符，默认为 空

    func simulate_day(season, weather, temperature, verbose):
        执行一整天的多智能体用电行为与负荷模拟。
        1. 初始化当天日期字符串。
        2. 根据当前家庭结构、世界元数据 and 日期创建一个全新的 Planner 计划器。
        3. 调用计划器生成当天的宏观活动计划（第一阶段：生成宏观计划）。
        4. 调用计划器进行多成员之间的活动协调（第二阶段：渐进式协调），解决空间与独占资源冲突。
        5. 调用计划器丰富每个成员的活动细节与情境描述（第三阶段：丰富行为描述）。
        6. 创建 Executor 执行器，针对每位家庭成员根据其性格、习惯 and 家电清单，进行家电使用批量决策（第四阶段：执行用电模拟）。
        7. 创建 EnergyCalculator 能耗计算器，对所有设备决策进行物理能耗积分，输出分钟级家庭负荷数据（第五阶段：计算用电信息）。
        8. 汇总模拟环境元数据、决策日志与能耗指标，追加至历史历史记录 history 中。
        9. 更新模拟时间并返回当天模拟的详细结果字典。

    func simulate_days(num_days, season, weather, temperature, verbose):
        执行连续多天的环境模拟。
        1. 遍历指定的总天数 num_days。
        2. 调用 simulate_day 执行单日仿真并收集结果。
        3. 在单日结束后，调用 time.next_day() 步进到下一天。
        4. 最终返回所有天数的模拟结果列表，并输出统计摘要。

    func next_day:
        将当前世界的时间推进一天，并自动更新日期类型（工作日/周末）。

    func prev_day:
        将当前世界的时间回退一天，并自动更新日期类型。

    func set_date(date_str):
        解析日期字符串并强制设置当前世界的时间。

    func add_holiday(date_str, holiday_name, description):
        向当前世界的时间管理器中注册特定的法定节假日信息。

    func add_special_event(date_str, event_name, description):
        向当前世界的时间管理器中注册特定的外部干预或气候灾难等特殊事件。

    func get_history:
        获取当前世界已完成的所有模拟天数的历史数据。

    func get_total_tokens:
        查询 SubAgent 统计的全局大模型 Token 消耗，包括 Prompt 缓存未命中、缓存命中、输出 Token 数量及总计。

    func reset_tokens:
        清空 SubAgent 中的大模型 Token 统计计数器。

    func print_summary:
        向终端控制台打印整个世界的模拟历史摘要、Token 消耗账单及每日运行日志路径。
