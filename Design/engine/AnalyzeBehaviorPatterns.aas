class AnalyzeBehaviorPatterns
    行为模式迁移分析（计划42，Jin 2021 household-days 聚类，0 LLM）。

    - scan_household_days(world_id, scenario) → 每户每日曲线样本
      [{house_id, date, kwh, shape, hourly}]（shape=24h 归一化）
    - build_report(samples, k) → 报告：
      clusters[]（中心形状 + house_days 成员列表）、
      per_house[]（cluster_sequence 逐日簇标签 / transitions 迁移次数 /
      dominant_cluster 主导簇 / mean_kwh）、
      mean_transitions（平均迁移次数）
      kmeans 退化时降级 k=1
    - CLI：python analyze_behavior_patterns.py <world> --scenario --k
    - 输出：outputs/<world>/analysis/patterns_<scenario>.json

    调用链：make_analysis_all 一键化；server
    /api/worlds/<id>/patterns/<scenario> 供前端"行为模式迁移"面板
    （每户簇序列彩色格子，一列一天）
