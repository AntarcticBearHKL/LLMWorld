class EnergyCalculator
    home 关联的家庭对象
    log_dir 仿真日志物理目录，默认为 空
    energy_info_dir 存储能耗明细输出结果的物理子目录，默认为 空
    appliance_usage 缓存电器ID到累积能耗明细映射的字典，默认为 空字典

    func load_all_decisions:
        扫描日志目录中的所有批量电器决策 JSON 文件，加载并反序列化为决策数据列表返回。

    func calculate_all_energy:
        第五层规划：收集全天所有成员的操作行为并结算整个家庭中所有电器的物理用电情况。
        1. 载入所有成员的批量决策记录。
        2. 遍历每个操作时间段，解析起止分钟数。
        3. 对每个家电的每次操作行为（use/charge_home），结合额定功率与状态时间段积分计算实际能耗（kWh）。
        4. 标记该电器在 1440 分钟内的分钟级工作状态位。
        5. 将能耗明细（含发生时间、时长、成员名字、能耗值等）追加至该电器使用统计中。
        6. 在全部处理结束后，调用 `_save_energy_info` 将各类汇总数据写入磁盘。

    func _save_energy_info:
        将统计计算结果持久化写入磁盘。
        1. 为每个发生过能耗的电器生成单独的 JSON 运行明细报告，详细列明其运行分钟轨迹、操作时段、以及累计电量。
        2. 生成一份全局的 "总用电汇总.json"，包含总家电数量、总耗电量（kWh）以及各家电能耗排行，用于前台可视化或政策弹性评估。

    func get_summary:
        快速返回全局用电量的简要汇总字典，按照耗电量降序排列。
