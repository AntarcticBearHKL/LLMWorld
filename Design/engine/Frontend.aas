class Frontend
    浏览器可视化前端（Implement/frontend/index.html，单文件、零依赖、实用清晰）。

    面板：
    1. 工具栏：世界下拉 / 日期下拉 / 刷新按钮 / 状态提示。
    2. 场景选择：checkbox 多选（默认前 5 个），支持同图叠加对比。
    3. 摘要卡片：总用电/户均/峰值/峰值时刻/家庭数/天气（environment 字段）。
    4. 负荷曲线：canvas 绘制多场景 24h 小时均值曲线（分色+图例+峰值点）。
    5. 多日能量轨迹：全场景 × 全日期分组柱状图。
    6. 逐户用电明细表。
    7. 场景对比矩阵表（policy_matrix.json）。
    8. 上帝剧本面板：新闻列表 + 上帝注入表单（POST /api/worlds/<id>/events）。
    9. 自动刷新：每 10 秒轮询当前视图（世界变动实时可见）。

    数据来源：server.py 的 /api/* 接口（同源）。
