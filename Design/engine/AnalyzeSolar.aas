class AnalyzeSolar
    光伏自用分析（计划71/72，Gunkel 2023 产消者，0 LLM）。

    - solar_generation_curve(season, weather, capacity) → 1440min 发电
      墨尔本日照模型：季节日出日落 + 天气系数（晴天1.0/多云0.6/
      阴天0.3/阵雨0.15）+ 正弦日内曲线（<1W 截断）
    - battery_operation(load, gen, capacity_kwh, power_kw) → 充/放曲线
      光伏富余充电（容量/功率双限）；17:00-22:00 晚峰优先放电
    - build_report(profiles, capacity, season, battery_kwh, power_kw) →
      每户：self_consumption_rate（含电池放电计入自用）/
      solar_coverage / grid_import / grid_export / battery_cycle_kwh /
      peak_import_cut_pct / net_peak_watts
    - CLI：python analyze_solar.py <world> --scenario [--date]
      [--capacity 5] [--battery 0] [--battery-power 3]
    - 输出：outputs/<world>/analysis/solar_<scenario>_<date>.json

    调用链：make_analysis_all 一键化；server
    /api/worlds/<id>/solar/<scenario>/<date> → 前端"光伏自用分析"面板
