class HouseholdMemory
    days 每日结构化摘要列表，默认为 空列表
    last 最近一天的行为摘要，用于注入次日宏观计划 prompt，默认为 空

    func update_from_day(day_result):
        每天模拟结束后从 World.simulate_day 返回的单日结果生成摘要并归档。
        1. 调用 _build_summary 汇总当日成员活动与家庭用电要点。
        2. 将摘要追加至 days 历史列表，并更新 last 为最近一天摘要。

    func _build_summary(day_result):
        使用纯本地逻辑（0 额外 LLM 调用）构建当天结构化摘要字典。
        1. 遍历 planner 中每位成员的时间线，调用 _summarize_member 提取活动概要。
        2. 调用 _summarize_electricity 汇总家庭用电要点。
        3. 返回包含日期、成员摘要、用电摘要三部分的字典。

    func _summarize_member(timeline):
        提取单个成员一天的行为特征。
        1. 遍历时间片，识别首个非睡觉活动段的开始时间作为起床时间。
        2. 识别最后一个含"睡觉/睡眠/入睡"活动段的结束时间作为入睡时间。
        3. 收集最多 8 段主要活动概要（起止时间-地点-活动），全天无活动时取首段兜底。

    func _summarize_electricity(day_result, energy_summary):
        汇总当日家庭用电要点。
        1. 读取总用电、基载与决策耗电（kWh）。
        2. 从能耗计算器的 1440 分钟家庭负荷曲线中定位峰值时刻与峰值功率。
        3. 取耗电量最高的前 3 台电器作为主要耗电清单。

    func get_prompt_context:
        生成注入次日第一层计划 prompt 的"昨日记忆"章节文本。
        1. 若无昨日记忆则返回空字符串，调用方需容忍空。
        2. 逐成员列出昨日活动概要（最多 8 段）与作息（起床/入睡）。
        3. 附上家庭昨日总用电、高峰时刻与主要耗电电器，形成"习惯连续性"。

    @静态方法
    func _fmt(minutes):
        将"从零点起的分钟数"格式化为 HH:MM 文本。
