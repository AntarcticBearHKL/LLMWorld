class AnalyzePolicyTradeoffs
    政策权衡分析（计划99，CoRenew 2026 competing objectives，0 LLM）。

    - _to_float(text) → 解析 "27.0 (-10.0%)" 与 "-8.5%" 两种格式
    - build_report(world_id) → 每政策多目标效果：
      total_change_pct / peak_hours_change_pct / peak_load_cut_pct /
      peak_plateau_cut_pct / peak_to_mean
      数据源：outputs/<world>/comparison/policy_matrix.json
    - CLI：python analyze_policy_tradeoffs.py <world>
    - 输出：outputs/<world>/analysis/policy_tradeoffs.json

    用途：政策画像对比（削峰 vs 节电 vs 错峰取舍）。

    调用链：server /api/worlds/<id>/policy-tradeoffs
    → 前端"政策权衡"面板
