class BaseAppliance
    name 电器类型名称（如 "空调"），默认为 空
    power_watts 额定功率（瓦），默认为 0
    appliance_type 硬件电力特性分类（"on_demand" / "charging" / "always_on"），默认为 空
    brand 品牌名称，默认为 空
    age 使用年限，默认为 0
    is_exclusive 是否为家庭独占排他性资源，默认为 否
    location 放置房间的物理名称，默认为 空
    owner 归属的特定家庭成员姓名，默认为 空
    location_id 房间标识符，默认为 空
    owner_id 成员标识符，默认为 空
    usage_log 记录该电器被使用及能耗情况的列表，默认为 空列表
    unique_id 自动生成的全局唯一标识符，默认为 自动计算值

    func _generate_unique_id:
        根据 location_id/location、owner_id/owner 以及 name 进行下划线拼接，生成系统中独一无二的家电ID。

    func _to_snake_case(text):
        辅助方法，将文本清理并转换为小写下划线命名规范（snake_case）。

    func calculate_energy(start_minutes, end_minutes, power_source):
        计算该家电在指定起止分钟数内的物理能耗。
        如果电力来源为 "external"（外部充能，如在公司充车），则不计入本家庭的总耗电量，直接返回 0。
        否则，换算为小时数并调用 `_calculate_energy_logic` 计算能耗，最后返回计算得出的电量（kWh）。

    func _calculate_energy_logic(duration_hours):
        默认的物理能耗乘积逻辑：功率（W） / 1000 * 小时。

    func get_available_actions:
        抽象方法，由子类重写。返回该家电允许大模型操作的行为指令列表。

    func get_config_schema:
        抽象类方法，由子类重写。返回该家电的属性 JSON schema 定义及物理参数边界。

    func from_config(config, location, owner, location_id, owner_id):
        抽象类方法，由子类重写。根据配置参数和绑定关系实例化对应的家电对象。

    func log_usage(start_time, end_time, energy_kwh):
        将本次家电使用起止分钟数、消耗电量及外部参数记录到 usage_log 列表中。

    func get_total_energy:
        遍历 usage_log，求和并返回该电器的累计消耗电量（kWh）。

    func to_dict:
        将电器的核心基础物理参数与允许操作的行为列表打包转为字典，为大模型决策输入提供支持。


class OnDemandAppliance
    @继承(BaseAppliance)
    func get_available_actions:
        按需即用型电器，返回可执行操作行为：["use", "idle"]。

    func to_dict:
        在父类字典的基础上，扩展说明文档：“使用时才耗电的设备（可开关）”。


class ChargingAppliance
    @继承(BaseAppliance)
    func get_available_actions:
        可充电式电器，返回可执行操作行为：["charge_home", "charge_external", "use", "idle"]。

    func to_dict:
        在父类字典的基础上，扩展说明文档：“充电设备（可使用家庭电力或外部电力充电）”及各个操作的具体消耗逻辑。


class AlwaysOnAppliance
    daily_energy_kwh 日均额定总能耗，默认为 (额定功率/1000)*24

    @继承(BaseAppliance)
    func _calculate_energy_logic(duration_hours):
        重写用电算法，将日均额定总能耗平均到小时上，返回 (daily_energy_kwh / 24.0) * 小时。

    func get_available_actions:
        持续常开型电器（如冰箱），返回空的行为列表，代表无需大模型人工决策。

    func to_dict:
        扩展说明文档：“持续耗电设备（无需操作，自动运行）”。


@可序列化
class AirConditioner
    @继承(OnDemandAppliance)
    func get_config_schema:
        返回空调配置约束：支持品牌 brand、功率 power（范围 1000W - 3500W）、使用年限 age。


@可序列化/独占资源
class ElectricVehicle
    @继承(ChargingAppliance)
    func get_config_schema:
        返回电动汽车配置约束与独占排他性规则（同一时间只能一人使用，使用者负责开出和归还，且支持同乘）。


@可序列化
class Refrigerator
    @继承(AlwaysOnAppliance)
    func get_config_schema:
        返回冰箱的物理参数配置：功率 power（范围 80W - 200W）、日耗电量 daily_energy_kwh（范围 0.5 - 3.0 kWh）、使用年限 age。


@可序列化
class TV / RiceCooker / Microwave / InductionCooker / RangeHood / Light / Lamp / Computer / Phone / WaterHeater / WashingMachine / VacuumCleaner
    @说明
    其他家电均继承自 OnDemandAppliance，并根据各自的额定功率、品牌进行相应的 get_config_schema 定义及构造初始化。
