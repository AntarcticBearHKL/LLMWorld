# 执行40:负荷曲线聚类与行为分组

## 实际做了什么

1. **论文搜索(持续开发模式第一轮)**:
   - arXiv 搜索 "large language model agent-based simulation" 与
     "electricity load profiling household"
   - 选定 3 篇依据(见计划40)
2. **规则更新**:§一.20 持续开发模式 + §一.21 更新必须有来源
   (用户 2026-08 追加指令,硬性:禁止凭空捏造功能,计划/执行必须体现思考过程)
3. **新增 `Implement/engine/load_features.py`**(纯逻辑,零注释):
   - `hourly_means` / `normalize_shape` / `load_factor` / `peak_hour` /
     `valley_hour` / `peak_to_mean`(负荷形状特征族)
   - `kmeans`(k-means++ 初始化,Arthur & Vassilvitskii 2007;
     空簇重初始化用全局最远点)+ `elbow_scores` + `auto_k`(肘部法)
4. **新增 `Implement/load_profile_cluster.py`**(CLI):
   - 扫描 `outputs/<world>/<postcode>/<house_id>/<scenario>/<date>/用电信息/`
     每户 1440min 曲线;`--date` 缺省取每户最后一天
   - `--k` 缺省肘部法自动选(2..min(8,户数))
   - 输出 `outputs/<world>/analysis/clusters_<scenario>_<date>.json`:
     每簇中心形状/户列表/平均负荷率/峰均比/平均峰时/谷时 + 每户标签 + WCSS 肘部
5. **`server.py`**:新增 `load_clusters` + `GET /api/worlds/<id>/clusters/<scenario>/<date>`
6. **前端**:新增"负荷曲线聚类"面板——每簇独立 canvas 画归一化中心曲线
   (峰值时刻打点),簇特征表 + 簇内家庭列表
7. **测试**:TestLoadFeatures(7 项)+ TestClustering(5 项)→ 总 90 项全绿(skipped=5 数据依赖)

## 结果

- `test_offline.py`:90 项 OK(skipped=5),0 失败
- CLI 冒烟(合成 3 户:晚峰/早峰/平缓):自动选 k=3,WCSS=0.0,
  三簇完全还原构造形状(峰时 19/8/5,负荷率 0.23/0.22/0.65)
- API 冒烟:重启服务器后 `GET /api/worlds/__smoke/clusters/baseline/2026-04-21`
  返回正确(已清理冒烟世界)

## 偏差

- 初始 k-means(随机均匀初始化)在 3 组可分离合成数据上出现空簇
  (只收敛 2 簇)→ 改用 k-means++ 初始化 + 空簇最远点重初始化,3 簇完全还原
- 测试断言写错一处(18 样本肘部应为 7 条而非 2 条),修正

## 下一步候选(计划 41)

1. **按簇定向政策实验**(Dent 2014:高变异性家庭对激励更敏感;
   Michalakopoulos 2023:聚类驱动定向 DR)——聚类结果接入政策对比:
   按簇分组的政策响应差异分析
2. **多日行为变异性指标**(Dent 2014:跨天行为变异性聚类)→ 需要多日数据,
   可先做工具:`load_features` 加跨天变异性特征
3. **前端增强**:聚类面板显示 WCSS 肘部曲线 + 政策响应×簇矩阵
