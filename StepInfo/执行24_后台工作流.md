# 执行 24 —— 后台工作流 + 解除并发限制 + 世界 10 户上限

> 对应 `计划24_后台工作流.md` + 用户 2026-08 最新指令。

## 实际做了什么
1. **background_runner.py**（后台任务管理器）：start/status/watch/stop；进度解析（第X/Y天/完成标记/错误）；`-X utf8` 修复 Windows GBK 输出乱码导致的状态误判（调试发现：子进程 stdout 重定向默认 GBK，中文完成标记匹配不上）
2. **解除 API 并发限制**（用户最新指令）：
   - subagent.py：移除全局信号量与 MAX_WORKERS；ThreadPoolExecutor = len(prompts)（每 prompt 一线程）；并发峰值保留为纯观测
   - config.py：删除 MAX_WORKERS（历史"并发≤10"要求作废）
   - population_runner.py：删除 MAX_HOUSEHOLDS_PARALLEL=5 → 户级并行 = len(worlds)（世界有多少户就并行多少户）
3. **世界级约束**：每世界最多 10 户（population.build_population 校验，超限 ValueError）
4. **工作流修正**（按用户要求）：模拟监听交给 subagent（Task），主 agent 持续开发；禁止 Start-Sleep 空转

## 验证结果
- **58 项测试全绿**（更新了旧并发约束测试）
- 10 户限制：`build_population('x', 11)` → 拦截 OK
- 状态机：非法命令→failed、aggregate-only→done、running 中间态→running（伪造日志三例验证）
- **pop03 基线（12 户 v2 Big Five）后台运行**（job_001，22 分钟，subagent 监听汇报）：
  - 总 177.69 kWh | 户均 14.81 | 中位 10.37 | 标准差 10.67 | 峰值 27170W@18:15
  - Token 1,140,880（户均/天 95,073）| 无警告无错误
  - 逐户跨度 6.5~39.4 kWh（8 类家庭用电异质性显著：多代同堂 39.4 / 退休夫妇 6.5）
- 并行开发产出（等待期间）：README.md 完整版、Design 4 个 .aas（EnvironmentInterface/BackgroundRunner/Frontend/Population v2）

## 与计划的偏差
- 计划 24 内 6 项全部完成；额外：GBK 编码修复（后台日志可解析的前提）、并发限制解除（用户新指令）

## 注意
- job_001 日志中"历史峰值并发 10（硬上限 10）"是**旧代码**输出（任务启动于解除限制前）；解除后的运行不再有该限制
- 00_需求与规则.md 已升级为完整存档（含进度里程碑/模块地图/命令速查/下一步候选），无 context agent 可据其无缝接续

## 下一步候选
见 00_需求与规则.md §六：pop03 人格多样性分析 → pop03 政策/新闻实验 → 多世界联合 → 前端增强 → 补执行21 → 论文报告 v2
