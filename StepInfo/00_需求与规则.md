# StepInfo —— 开发需求与规则（最高优先级存档）

> **本文件是"游戏存档"**：一个没有任何上下文的 agent，只需 ① 读本文件 → ② 按编号读 `StepInfo/计划N_*.md` 与 `执行N_*.md` → ③ 读 `README.md` → ④ 看 `git log --oneline -20`，即可无缝接续当前开发进度。
> **每步规划/执行前必须重读本文件。**

---

## 零、当前状态快照（读档入口）

- **当前轮次**：33 轮进行中（git 提交 30+，HEAD 见 `git log`）
- **测试**：`python Implement/test_offline.py` 64 项全绿
- **活跃世界**：worlds/373、495（LLM 生成）、pop01（4 户 v1）、pop02（10 户 v1）、pop03（12 户 v2 预设）、pop04（10 户 v2 去作弊化，seed 11）
- **pop04 连续时间线**（研究主线）：D1=04-21（92.1kWh）→ D2=04-22（90.0kWh）→ D3=04-23 TOU（90.6kWh，同日对比**总-8.98%/晚峰-19.38%**）→ D4=04-24 新闻实验（job_009 运行中）
- **服务器**：系统级常驻（`background_runner server status` 查看，PID 记录于 logs/jobs/server.pid）
- **后台任务**：`background_runner status` 查看；历史 job_001~009
- **最新结果亮点**：去作弊化实证（pop04 vs pop03 户均 -37.8%，EV 预设贡献 23kWh/天）；续跑验证（第 2 天 10/10 记忆注入）；思考强度 low（8.9s vs high 3min）；22 户联合人口基线相关 **0.7079**（历史最高）
- **最近的指令变更**：见 §一.4/9/12/13/14/15
- **下一步候选**：见 §六

---

## 一、用户原始要求（2026-08 会话确立，持续有效）

1. **持续开发**：一直重复地开发下去，直到用户手工终止进程。永远不要停。
2. **代码可读性**：所有代码架构逻辑清晰，不懂代码的人也能一眼看懂。分层明确、命名直白、中文注释鼓励、单一职责。
3. **学术导向**：尽可能去任何论文网站（arXiv/Google Scholar/ACM/IEEE）搜索想法并合理应用；执行记录中注明论文出处（名/作者/年）与应用原理。
4. **API 约束（已按 2026-08 最新指令更新）**：
   - 只使用项目已有的 DeepSeek API 做测试（.env 的 DEEPSEEK_APIKEY）
   - 【最新】API 并行请求**不设任何上限**；户级并行多少只取决于世界有多少家庭（population_runner 的 ThreadPoolExecutor = len(worlds)）
   - 【世界级约束】每个世界**最多 10 户**（population.build_population 校验，超限抛 ValueError）
   - 重试机制保留（3 次指数退避）；并发峰值仅作观测（SubAgent.get_concurrency_stats），不做限制
   - **思考强度最低（2026-08 指令）**：`config.REASONING_EFFORT = "low"`（官方枚举仅 low/high/max，见 api-docs.deepseek.com/guides/thinking_mode；flash 传 low=实际最低档）。注意：**官方不识别 medium**（曾被忽略→实际默认 high）。思考模式下 temperature 等参数不生效，请求中已移除
   - 历史遗留：早期曾要求并发 ≤10（全局信号量）——**已作废**，但旧运行日志（job_001）中的"峰值 10"是旧代码输出，不代表现状
5. **数据约束**：使用的数据尽可能围绕 **3168 / Clayton**（墨尔本）；数据文件统一在 `Data/` 目录（census CSV、vic_electricity_data.csv、工具脚本 Data/tools/）
6. **开发节奏**：走一步看结果 → 没问题再规划下一步 → 执行。每步必须有"验证"环节（能跑就真跑，不能跑说明原因）。永远保留改进空间。
7. **记录要求**：`StepInfo/计划N_*.md` + `执行N_*.md` 持续记录（计划写"做什么/为什么/怎么做/怎么验证"，执行写"实际做了什么/结果/偏差/下一步候选"）。
8. **改动边界**：**只允许在 LLMWorld/ 内创建/修改文件**；每步执行完必须 git 提交同步。
9. **后台工作流模式（2026-08 最新指令，替代旧式阻塞等待）**：
   - 模拟任务放后台：`Implement/background_runner.py start <world> <args...>`（Popen 子进程 + logs/jobs/job_NNN.out）
   - **subagent 负责启动并监听模拟**（Task 工具派 general agent：轮询 `background_runner.py status`，最多等 40 分钟，完成后汇报聚合结果/token/警告）
   - **主 agent 在 subagent 监听期间持续开发**，累积变更（与模拟不冲突：模拟进程已加载代码到内存，改文件不影响运行中进程）
   - subagent 返回结果后：主 agent 根据结果追加变更（分析/报告/前端）
   - 最后把**等待期间累积的全部变更**交给新的 subagent 统一测试（跑 test_offline 等）
   - **禁止用 Start-Sleep 空转阻塞主线程**
10. **前端同步**：`Implement/frontend/index.html`（浏览器）+ `Implement/server.py`（常驻后端，HTTP API + CLI 查询）——**每一步新功能必须同步更新到前端展示**；界面实用清晰即可，不追求美观。
11. **环境信息接口**：天气/气温等 → `engine/environment_interface.py` 三模式（`config.ENV_MODE`）：real（真实 API，失败回退 config）/ config（墨尔本气候表按季节随机，种子可复现）/ manual（`Implement/env_manual.json` 固定值或范围）。
12. **世界日期连续（2026-08 最新指令）**：一个世界是连续时间延续。
    - 刚创建的世界没有日期（未开始模拟）→ 用 config.DEFAULT_START_DATE 开始
    - 每天模拟结束保存 `worlds/<id>/state.json`（当前日期 + 跨天记忆 memory_days）
    - 之后开始模拟时若未显式指定 --date → **自动读取 state.json，从上一次日期的下一天继续**，并恢复记忆（昨天的行为影响今天）
    - 实现：`World._resume_date/_load_memory_days/save_state`、`HouseholdMemory.load_days`；simulate.py / population_runner.py 的 --date 缺省即续跑
13. **生成去作弊化（2026-08 最新指令）**：世界/家庭/人物生成时**不得出现任何与用电行为强相关的词条**：
    - 禁止：直接写明是否使用智能家居/电动车（population.py 已移除 EV 预设 with_ev）、energy_awareness（节能意识）字段、省电/关灯类行为表述
    - 已清理：`big_five_to_energy_awareness` 删除、BIG_FIVE_TEXT 中性化（"乐于尝试新鲜事物"等）
    - 原则：用电行为必须由 LLM 在模拟中从人格/情境**自发涌现**，生成阶段预设即"作弊"
    - 测试保障：`test_no_energy_awareness_in_generation`（生成 blob 不得含 节能/省电/电动汽车/关灯）
    - 兼容：旧世界（pop02）仍有 energy_awareness 字段；analyze_groups 对缺失字段标记"未知"
14. **服务器常驻（2026-08 最新指令）**：只要开始开发，就**系统级启动可视化服务器**（`background_runner server start`，Popen 后台进程）；**每次前后端修改后立即重启服务器**（`server restart`）。
15. **禁止等待进程（2026-08 最新指令，硬性）**：
    - **任何情况下不允许使用 Start-Sleep 或等效阻塞等待**（包括等模拟、等测试、等 API）
    - 所有需要等待的行为一律转**系统级后台进程**（background_runner start / server start）
    - 检查进度用一条快速命令（status / 读日志尾部），跑完再汇总
    - 即使所有任务似乎"必须等结果才能继续"，也必须改为：先优化当前代码/项目结构/写文档/做离线可做的工作，绝不空等
    - 禁止用宽泛进程匹配清理（教训：`Get-Process python | Stop-Process` 误杀过后台模拟 job_003），清理必须按精确 PID

---

## 二、项目进度里程碑（计划 1-24）

| 计划 | 主题 | 关键结果 | 提交 |
|---|---|---|---|
| 1 | 能耗可信化 | 常开基载计入日总用电；house_load_profile_1440min.json；决策校验 warnings；并发≤10+重试（旧）；utils 抽公共；14 测试 | dd01087 |
| 2 | 配置化+API 冒烟 | config.py；smoke_test.py；**thinking 90x 提速**（开=3min/条 vs 关=2s/条）→ config.THINKING 默认 False | cfa7f12 |
| 3 | 跨天记忆 | memory.py 本地摘要（活动/作息/用电要点）→ 第 2 天 prompt 注入昨日记忆；JSON 解析自修复 | 762cf18 |
| 4 | 人口级模拟（RQ1） | population.py 模板构建；population_runner 并行+聚合；全局信号量（旧）；4 户验证：家庭规模效应可见（3人 24.2 vs 独居 4.4 kWh） | d3fe8a4 |
| 5 | 真实数据基线（RQ3） | validate_baseline.py vs VIC1：晚峰偏移 0-1h、相关性 0.62（4户）、峰均比 4.83 | 0da5279 |
| 6 | 10 户规模验证 | 峰均比 4.83→2.52（N=4→10 平滑）；**发现 EV 充电滥用**（house_0010 107kWh） | 306720e |
| 7 | 决策真实性约束 | config.APPLIANCE_DAILY_CAP_MINUTES 14 类电器日上限+截断警告；prompt 典型时长表；修复后 house_0010 107→29.7kWh、相关性 0.35→0.56 | 306720e |
| 8 | TOU 政策实验（RQ2） | policy.py；compare_policies.py；**晚峰时段-28.95%、峰值-36.7%、谷段+145%**；总用电+19.8%（低谷诱导） | acef855 |
| 9 | nudge 实验 | 三场景矩阵：nudge 峰值-8% < TOU -31%（Pellerano 方向）；修复聚合路径覆盖缺陷（按场景子目录） | 98268e3 |
| 10 | 节能意识分组 | analyze_groups.py：**高意识 TOU -15.7% vs 低意识 +5.2%**（Costa&Kahn 2010 重现）；EV 诱导充电异质性 | 5f767bf |
| 11 | 政策矩阵补全 | nudge_loss（损失框架）**唯一总量下降 -5.5%**（Ghesla 2019 几乎定量吻合）；subsidy 谷段+57.8% 但总量+26.3% | 038b321 |
| 12 | 论文素材报告 | make_report.py：价格弹性 **-0.161**（文献区间 -0.05~-0.5）；reports/paper_material_pop02.md | 156a93b |
| 13 | 新闻/上帝模式 | news.py（NewsBoard/events.json/--event/POST）；step1+step4 注入 {world_news}；**并行开发首用**（2 subagents）；495 验证：空调附加税→全天空调仅 1.0kWh | 156a93b |
| 14 | 人口级新闻实验 | war_news：**峰值-29.2% ≈ TOU 的-31.1%**（新闻预期传导=价格工具量级，RQ2 核心证据） | cf3774b |
| 15 | 上帝控制台+多日连载 | god_cli.py 交互注入；495 两天：记忆×新闻协同（第 2 天计划含昨日记忆+返利新闻） | b732e35 |
| 16 | 可信度实验 | rumor_news 场景（社交媒体谣言 vs 官方，--no-events + --event） | 5f767bf 后 |
| 17 | 数据目录整理 | 根目录数据/脚本 → Data/、Data/tools/；引用路径修正 | 78b197c |
| 18 | 可视化（前后端分离） | server.py（常驻 HTTP+CLI）+ frontend/index.html；零依赖 | 04e9090 |
| 19 | 前端上帝控制台 | POST /api/worlds/<id>/events 注入 + 10s 自动刷新 | f13f9bb |
| 20 | 前端增强 | 多场景曲线叠加 + 多日能量轨迹；基线数据迁移到场景目录 | 60cbd5f |
| 21 | pop02 多日连载 | war_series 3 天（战争爆发→升级→停火）：总 169.6kWh(第3天)，峰值 34130W@17:35；2.87M tokens | （执行21未写，见计划21）|
| 22 | 世界生成 v2 | **联网调研 12 篇 arXiv**；Big Five 五维人格+行为描述；8 类家庭（+多代/单亲/退休/国际学生）；属性关联链（年龄→职业→收入→住房→电器）；fringe 配额；姓名去重；54 测试 | 05c1e76 |
| 23 | 环境信息接口 | environment_interface.py 三模式；MELBOURNE_CLIMATE 气候表；前端天气卡片；58 测试 | c5cdc6f |
| 24 | 后台工作流+解除并发 | background_runner.py（start/status/watch/stop）；**API 并发不设上限**；世界≤10 户；户级并行=世界户数；pop03 基线后台运行完成（见 §零） | b955285 |
| 25 | 人格多样性分析 | analyze_population.py：成员数×用电 +0.503（规模效应最强）；类型/意识标签混杂（方法学发现）；前端分析面板；62 测试 | step25b |
| 26 | 多世界联合聚合 | combine_worlds.py（0 token 合并 → combined_pop02+pop03 22 户）；server 虚拟世界支持（仅 outputs 实体）；62 测试 | step26 |
| 27 | 世界日期连续+去作弊化 | state.json 断点续跑（上次日期+1 天+记忆恢复）；生成移除 EV/节能意识/省电词条；pop04（10 户去作弊化）后台 baseline 运行中；63 测试 | step27 |
| 28 | 服务器常驻+禁等待 | background_runner `server start/restart/stop/status`（系统级常驻）；前端后台任务监控面板（/api/jobs）；server 模块级导入优化；§一.14/15 入档 | ec7f2c2 |
| 29 | 去作弊化实证 | pop04 无预设户均 9.21 vs pop03 预设 14.81 kWh（**-37.8%**）——预设直接决定用电量级，作弊担忧证实；state.json 按户存储+原子写（修复多户覆盖）；pop04 续跑实战（job_005）| step29 |
| 30 | 前端人格档案 | /api/worlds/<id>/households（成员/年龄/职业/五维）；前端 Big Five 面板；check_resume 工具 | step30b |
| 31 | 思考强度最低 | **DeepSeek 官方文档修正**：reasoning_effort 仅 low/high/max（medium 非法被忽略→实际 high）；改为 **low**（flash 最低档，8.9s/条 vs high 3 分钟）；思考模式不传 temperature | 5d57d87 |
| 32 | 续跑验证闭环 | pop04 第 2 天自动续跑：**10/10 成员昨日记忆注入**；两天轨迹稳定（9.21→9.00 kWh）；check_resume + --dates 多日对比工具 | step32 |

**论文级发现汇总**（可写论文的核心结果）：
1. TOU 削峰 -31~37%（晚峰时段用电 -9~-29%），峰值时刻稳定移至 22:30
2. 损失框架 nudge 是唯一总量下降场景（-5.5%，Ghesla 2019 对照）
3. 新闻（战争+税收）削峰 -29.2% ≈ 价格工具（信息即政策）
4. 高节能意识家庭响应 -15.7% vs 低意识 +5.2%（Costa & Kahn 2010）
5. 峰段价格弹性 ≈ -0.161（文献区间内，Wang et al. 2021 方向）
6. 峰均比随 N 收敛（4.83→2.52），10 户仍高于真实 1.2（规模论证）

---

## 三、系统能力清单（模块地图）

```
Implement/
  engine/
    world.py               世界（时间/记忆/新闻台/每日五阶段管线/上帝API add_world_event）
    planner.py             第一~三层：宏观计划→渐进协调→丰富描述（memory/news 注入）
    executor.py            第四层：用电决策（policy/news 注入，决策校验）
    energy_calculator.py   第五层：能耗（基载+1440min曲线+日使用上限截断+警告）
    subagent.py            DeepSeek 调用层（并发不限/重试3次/LLMCallError/Token+并发峰值观测）
    memory.py              跨天记忆（HouseholdMemory 本地摘要→prompt）
    news.py                新闻台（NewsItem/NewsBoard/events.json/--event/API注入）
    policy.py              政策（tou/subsidy/nudge/nudge_loss）
    environment_interface.py 环境三模式（real/config/manual）
    generator.py           世界生成 LLM 版（地区→分布→家庭）
    weather_api.py         真实天气/节假日 API（含兜底）
    utils.py               公共工具（JSON/日志/时间/校验/parse_json_with_retry/set_seed）
  population.py            人口构建 v2（Big Five+8类家庭+关联链+配额+≤10户校验，0 LLM）
  population_runner.py     人口级并行模拟（户级并行=世界户数；--policy/--event/--scenario/--no-events/--aggregate-only）
  simulate.py              单家庭模拟入口（交互/--no-input；人格字段完整注入）
  background_runner.py     后台任务管理器（start/status/watch/stop；-X utf8）
  server.py                可视化后端（常驻 HTTP + --query CLI）
  frontend/index.html      前端（场景叠加曲线/多日轨迹/矩阵/新闻/上帝注入/10s刷新）
  config.py                配置中心（MODEL/THINKING/ENV_MODE/MELBOURNE_CLIMATE/APPLIANCE_DAILY_CAP_MINUTES）
  god_cli.py               上帝交互控制台（逐日注入事件）
  smoke_test.py            全管线冒烟测试（真实 API）
  test_offline.py          离线测试（58 项）
Design/                    .aas 架构文档（与 Implement 同步）
prompts/                   LLM 提示词模板（3 生成 + 4 模拟，含 {memory_context}/{world_news}/{policy_context}）
Data/                      census/电力数据 + tools/
worlds/<id>/               世界数据（household.json/events.json/district.json/world.json，gitignored）
outputs/<id>/population/   聚合结果（按场景子目录，gitignored）
reports/                   论文素材（make_report 生成）
logs/jobs/                 后台任务元数据
StepInfo/                  本存档 + 计划N/执行N
```

## 四、关键命令速查

```bash
python Implement/test_offline.py                      # 离线测试（58 项全绿）
python Implement/population.py pop04 --count 10       # 生成人口（≤10 户）
python Implement/population_runner.py pop03 --days 1 --scenario baseline   # 人口模拟
python Implement/population_runner.py pop03 --policy tou --scenario tou_x  # 政策
python Implement/population_runner.py pop03 --scenario war --event "2026-04-21|标题|内容"  # 新闻
python Implement/background_runner.py start pop03 --days 1 && status && watch job_001
python Implement/server.py                            # 前端（http://localhost:8080）
python Implement/server.py --query profile --world pop03 --scenario baseline --date 2026-04-21
python Implement/compare_policies.py --world pop03 --all
python Implement/analyze_groups.py --world pop03
python Implement/make_report.py --world pop03
python Implement/validate_baseline.py --world pop03
```

## 五、验证规范

- 纯逻辑（能耗/时间/聚合/人格映射/环境采样）→ `test_offline.py` 全绿（当前 58 项）
- 真实 API 流程 → `smoke_test.py` 或单户 `simulate.py --no-input`（小规模，记录 token）
- 后台长模拟 → background_runner + subagent 监听（§一.9）
- 每步结束：`git status` 检查 → 提交（commit message 以 stepN 开头）

## 六、下一步候选（计划 35+，按优先级）

1. **pop04 新闻实验结果**（job_009 后台运行中）：完成后启动同日基线（`start pop04 --days 1 --date 2026-04-24 --scenario baseline`）→ `compare_policies --world pop04 --intervention war_news --base-date 2026-04-24`
2. **pop04 四天连续时间线总结**（D1-D4 轨迹 + 政策/新闻对照）
3. 前端增强：多日轨迹面板（--dates 数据展示）
4. 论文报告整合 v6（去作弊化/续跑/同日对比/联合对齐章节）
