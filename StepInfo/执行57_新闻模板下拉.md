# 执行57:新闻模板下拉注入

## 实际做了什么

1. **依据**:00 规则 §七.2 遗留项收尾(运维经验)
2. **`server.py`**:`load_templates()`(遍历 news_templates
   template_names,NewsItem 属性取 title/content)+
   `GET /api/news-templates`(10 类模板)
3. **前端**:上帝面板加"新闻模板"下拉 + 填入按钮
   (自动填标题/内容,日期用户自定,可改后注入)
4. 测试:TestTimelineServer.test_load_templates_nonempty
   → 139 项全绿
5. 修一处 bug:build_template 返回 NewsItem 对象,
   sample.get → getattr(首次 500)

## 结果

- `test_offline.py`:139 项 OK(skipped=5)
- API 冒烟:10 类模板(ac_tax/blackout_risk/cold_snap/
  energy_crisis/heatwave/lockdown/price_hike/rebate/
  solar_incentive/storm)返回正常
- server 重启完成

## 偏差

- 模板对象属性访问方式修正

## 下一步候选(计划 58)

1. **世界日期时间线约束**(用户 2026-08 新指令):
   未开始可任意设日期;已开始只能顺延(+1 天)继续,
   禁止覆盖/回退/跳日;想重开时间线=新建世界 ID
