# 执行 17 —— 数据目录整理

> 对应 `计划17_数据目录整理.md`。

## 实际做了什么
1. 根目录数据全部移入 `Data/`：
   - clayton_3168_{demographics,economics,housing,education_employment,culture_habits}.csv ×5
   - vic_electricity_data.csv、clayton_translation_cache.json
2. 数据脚本移入 `Data/tools/`：integrate_census.py、test_openelectricity.py
3. 路径引用修正：
   - `validate_baseline.py` 默认真实数据 → `Data/vic_electricity_data.csv`
   - `integrate_census.py` 硬编码的 C:/Users/antar/Desktop/LLMWorld/... 路径 → Data/ 相对路径（BASE_DIR/OUTPUT_FILE/CACHE_FILE/glob）
   - `test_openelectricity.py` 输出路径 → Data/ 相对路径
4. 根目录现在只剩 .env 与 .gitignore

## 验证结果
- 44 项离线测试全绿
- `validate_baseline.py --world pop02` 正常：真实数据从 `Data/vic_electricity_data.csv` 加载，晚峰偏移 0 小时，相关性 0.5554（与移动前一致 → 无回归）
- git 提交（Data/ 已在 .gitignore，不入库）

## 下一步（计划18）
前后端分离的模拟世界可视化：浏览器前端 + 常驻后端服务（HTTP API + 命令行交互），并从此每步新功能同步更新前端。
