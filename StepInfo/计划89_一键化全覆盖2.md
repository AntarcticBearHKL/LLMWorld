# 计划89:一键化全覆盖第二轮(4 个新工具纳入)

## 做什么

make_analysis_all.py 纳入计划79/83/85/87 的 4 个新工具:
analyze_electrification(每场景最新日)/ analyze_advice(最新日)/
analyze_seasonal(多日)/ analyze_weather_sensitivity(多日),
与计划74 的一键化补齐,实现"一键分析=全部 18 工具"。

## 为什么(依据)

**运维经验(§一.21)**:计划74 一键化覆盖 11 个工具,
79/83/85/87 新增 4 个后浏览器一键分析不再完整。

## 怎么做

1. `make_analysis_all.py`:run_all 每场景追加
   - analyze_electrification(build_report 默认参数 →
     electrification_<scenario>_<date>.json)
   - analyze_advice(build_report → advice_<scenario>_<date>.json)
   - analyze_seasonal(多日 → seasonal_<scenario>.json)
   - analyze_weather_sensitivity(多日 → weather_<scenario>.json)
2. 测试:各工具测试已覆盖;一键化冒烟(合成世界)

## 怎么验证

- `python Implement/test_offline.py` 全绿
- 合成世界 make_analysis_all 冒烟(4 新 JSON 产物)
