# 执行50:前端一键分析(上帝控制台增强)

## 实际做了什么

1. **依据**:运维经验(用户手工模拟模式下分析碎片化)+
   Fidone et al. 2025 (arXiv:2511.07204)一键可复现评估工作流
2. **`server.py`**:新增 `POST /api/worlds/<id>/analyze`——
   同步调用 make_analysis_all.run_all(world_id)(redirect_stdout
   捕获输出),返回 {"ok", "output"} / {"error"}
3. **前端**:上帝剧本面板新增"一键分析当前世界"按钮 +
   状态提示(分析中→完成刷新/失败),完成后 refresh() 全部面板
4. 测试:122 项全绿(无逻辑变更)

## 结果

- `test_offline.py`:122 项 OK(skipped=5)
- 冒烟:server restart 后 POST /api/worlds/__smoke6/analyze
  返回完整分析输出(政策矩阵+各分析摘要),合成世界已清理

## 偏差

- 无

## 下一步候选(计划 51)

1. **LLM 模拟管线**:家庭间社交网络(同伴效应,CoRenew 2026 先例)
   ——引擎级大功能,需用户确认方向
2. **模式迁移 × 事件联动**:迁移次数与新闻/政策敏感度交叉
3. **前端政策选择器**:政策实验下拉(含组合)一键启动后台模拟
   (受 §一.16 约束:模拟需用户明确指示,前端按钮做成"确认后启动")
