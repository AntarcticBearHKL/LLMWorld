# 执行87:天气敏感性分析

## 实际做了什么

1. **论文依据**:Jin et al. 2021 (arXiv:2102.11027,
   温度是负荷变异最重要驱动,RefPaper 已存)
2. **新增 `Implement/analyze_weather_sensitivity.py`**:
   - `pearson` / `linear_slope`(最小二乘 kWh/°C)
   - `load_daily_points`:扫描聚合 JSON 的 environment 字段
     (温度取首户均值)+ 日总电
   - 按天气条件分组日均电
   - 输出 weather_<scenario>.json
3. **`server.py`**:`GET /api/worlds/<id>/weather/<scenario>`
4. **前端**:新增"天气敏感性"面板
5. **测试**:TestWeatherSensitivity 4 项 → 182 项全绿

## 结果

- `test_offline.py`:182 项 OK(skipped=5)
- 合成验证:正相关(空调主导)/负相关(采暖主导)/天气分组
- server 重启完成

## 偏差

- 无

## 下一步候选(计划 88)

1. **存档同步**(里程碑 86-87 入档 + Design)
2. **户间双向社交网络**:引擎级大功能,需用户确认方向
