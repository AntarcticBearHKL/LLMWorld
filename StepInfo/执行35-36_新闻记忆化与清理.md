# 执行 35/36 —— 新闻记忆化渐进 + 数据清理 + 前端任务 tab

> 依据：`StepInfo/00_需求与规则.md`（已重读）。

## 计划35：新闻记忆化渐进（用户指令）
### 实现
1. **NewsBoard 投递逻辑**：`delivered_ids`（id=日期_时间_标题）记录已投递；`get_new_for(date)` 只返回"未投递且发布日期<=当天"的新闻（后补旧闻未看过算新）；`to_state/restore_state` 落盘恢复
2. **HouseholdMemory 新闻记忆**：`news_memory` 滚动保留最近 `config.NEWS_MEMORY_KEEP=5` 条（去重）；`get_prompt_context()` 增加"近期外界信息回顾"章节（旧闻摘要形式）
3. **World 落盘**：`save_state` 增加 `news_delivery`（投递进度，世界级）+ `news_memory`（按户）；启动时 `_restore_news_state` 恢复 → 重启只投递新一天
4. **注入时机/上帝方式/占位符结构不变**（用户约束）
5. 测试：7 项新测试（首日/次日/后补/状态往返/记忆滚动/回顾章节/World 落盘）→ **71 项全绿**

### 验证（待 job_002 后）
- [ ] 连续多天模拟：第 2 天 {world_news} 只含当天新增、旧闻以摘要出现在记忆回顾
- [ ] 中途重启：投递进度与记忆无丢失/重复

## 执行36：数据清理（用户指令）+ 前端任务 tab
### 数据清理
- 保留：pop04（10 户 v2 去作弊化，主线）+ pop05（3 户 v2 去作弊化，新测试世界）
- 删除：worlds/373,495,pop01,pop02,pop03 + outputs 对应 + combined_* + logs/jobs 历史记录 + 过时 reports
- 判断依据：v1 结构/含 EV 与节能意识预设/12 户超限 → 不符合当前规范

### 前端任务 tab
- server: `POST /api/jobs/delete/<id>`（running 禁删）
- frontend: 任务面板 tab（进行中/完成/报错，默认进行中）+ 删除按钮
- 测试更新到 pop04 → 71 项全绿

## 运维发现：DeepSeek 超限速排队
- 20+ 并发时部分请求被服务端**排队**（5 并发中 2 个排队 4 分钟），timeout=120 误杀排队请求触发重试雪崩
- 修复：`REQUEST_TIMEOUT_SECONDS = 600`（排队请求有足够时间完成）
- job_002（pop04 war_news，timeout 600）后台运行中验证
