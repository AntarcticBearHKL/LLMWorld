# 执行 20 —— 前端增强：多场景叠加 + 多日能量轨迹

> 对应 `计划20_前端曲线叠加.md`。

## 实际做了什么
1. **场景多选叠加**：场景区改为 checkbox（默认前 5 个勾选）→ 同 canvas 绘制多条 24h 曲线（每场景一色 + 图例 + 峰值点标注）
2. **多日能量轨迹面板**：世界级所有场景 × 所有日期的总用电分组柱状图（场景分色 + 图例）
3. **数据迁移**：pop02 基线从 aggregate_only.json 迁移到 `population/baseline/2026-04-21/`（旧结构遗留，一次性修复）→ 前端场景列表现含全部 6 场景

## 验证结果
- 页面 200，含"多场景叠加"与"多日能量轨迹"面板
- baseline profile 正常返回：141.52 kWh，户均 14.15
- pop02 场景数 6：baseline, nudge_loss, rumor_news, subsidy, tou, war_news
- 47 项测试全绿（无后端改动）

## 与计划的偏差
- 无偏差 + 一次性数据迁移（基线场景目录化）

## 下一步候选（计划21）
1. **研究主线**：pop02 多日连载实验（战争升级→缓解 3 天，~260 万 token）——前端趋势图将有真实多日数据可看
2. 前端面板增强：跨天记忆状态、单户时间线
3. 模拟运行状态页
