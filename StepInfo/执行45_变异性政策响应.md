# 执行45:变异性 × 政策响应交叉验证

## 实际做了什么

1. **论文依据**(计划41/计划45已搜):Zhou et al. 2016
   (arXiv:1607.00595)+ Dent 2014(arXiv:1409.1043)
   "高变异性家庭对政策更敏感"主张的落地验证工具
2. **`analyze_groups.py` 扩展**:
   - `--label-source awareness|variability`(默认 awareness,旧用法兼容)
   - `load_variability_labels`:读 variability JSON 的
     regular_half→"规律"、variable_half→"波动"
   - `group_stats` 动态分组(不再硬编码 高/中/低/未知)
   - 输出 `outputs/<world>/analysis/groups_<source>.json`(+label_source)
3. **`server.py`**:`load_groups` + `GET /api/worlds/<id>/groups/<source>`
4. **前端**:新增"分组政策响应"面板——标签源下拉
   (节能意识/行为变异性),组×场景变化% 表格(负值绿色/正值红色)
5. **测试**:TestGroupAnalysis 新增 2 项(动态分组、
   variability 标签映射)→ 110 项全绿

## 结果

- `test_offline.py`:110 项 OK(skipped=5)
- CLI 冒烟(合成:规律户 h1/h2,波动户 h3):
  - 规律组 TOU 响应 -6.2%、波动组 -25.0%
  - 输出 groups_variability.json 正确
- server 重启完成,新端点就绪

## 偏差

- 冒烟脚本首次路径建错(variability JSON 误放 population/ 下),
  修正后通过——工具本身无 bug

## 下一步候选(计划 46)

1. **工具链一键化**:make_analysis_all.py 一次跑完
   (聚类/变异性/模式/归因/分组/基线)
2. **peak_demand 决策校验**:同小时大功率叠加警告入 warnings
3. **前端三面板联动**(聚类/变异性/模式,点击家庭高亮)
4. **变异性×聚类联动**:簇内变异性分布(用 Jin 2021 的驱动因素视角)
