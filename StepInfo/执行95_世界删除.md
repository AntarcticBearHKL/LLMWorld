# 执行95:世界删除

## 实际做了什么

1. **依据**:运维经验(世界/产物堆积无清理入口)
2. **`server.py`**:
   - `validate_delete_world` / `delete_world`:
     ID 校验 + 运行中任务拒绝 + rmtree(worlds/ + outputs/)
   - `POST /api/worlds/delete/<world_id>`
   - (修一处:编辑时误覆盖 validate_create_args,已恢复)
3. **前端**:工具栏"删除世界"按钮(confirm 二次确认)
4. **测试**:TestSimArgs 新增 delete_world_validation
   → 189 项全绿

## 结果

- `test_offline.py`:189 项 OK(skipped=5)
- API 冒烟:创建→删除成功,二次删除 400
- server 重启完成

## 偏差

- 编辑误覆盖 validate_create_args(已恢复,测试兜底)

## 下一步候选(计划 96)

1. **存档同步**(里程碑 94-95 入档 + README 工具栏)
2. **户间双向社交网络**:引擎级大功能,需用户确认方向
