# 计划/执行 26 —— 多世界联合聚合 + 虚拟世界前端

> 依据：`StepInfo/00_需求与规则.md`（已重读：每世界 ≤10 户约束下需要更大人口；每步新功能同步前端）。

## 为什么做
用户 2026-08 指令"每世界最多 10 户"。更大人口（20/30 户）通过**多世界联合**解决：各世界独立模拟后合并（0 token 纯聚合）。

## 实际做了什么
1. **`Implement/combine_worlds.py`**：`--worlds pop02 pop03 --scenario baseline --date ...` → 逐元素合并 1440min 曲线、per_house 带世界前缀（`pop02:house_0001`）、统计重算 → `outputs/combined_<w1>+<w2>/population/<scenario>/<date>/`
2. **server 虚拟世界支持**：`list_worlds()` 扫描 outputs/ 下无对应 worlds/ 目录的 population 实体（combined_*），前端世界下拉自动出现
3. 测试：combine 数学正确性（总用电=各世界之和、1440 点、前缀）→ **62 项全绿**

## 结果
- `combined_pop02+pop03`：**22 户**（pop02 10 + pop03 12），总 319.21 kWh，户均 14.51，中位 11.27，峰值 58,450W@18:00
- 前端可直接查看 22 户联合人口（曲线/矩阵/明细）

## 后台任务状态（job_002：pop03 TOU 实验）
- 运行中（约 3 分钟时处于最后几户丰富阶段），预计 15-25 分钟完成
- 完成后：compare_policies --all（pop03 矩阵）、人格×政策响应分析、前端矩阵刷新

## 下一步候选
- pop03 TOU/nudge/战争场景补齐 → 人格×政策异质性分析（控制家庭类型）
- 联合人口基线对齐（22 户 vs VIC1 真实：峰均比收敛验证）
- 执行26 归档（本文件即归档）
