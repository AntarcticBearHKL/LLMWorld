# 执行58:世界日期时间线约束

## 实际做了什么

1. **依据**:用户 2026-08 明确指令(禁止覆盖/回退/换时间,
   想重开=新世界 ID)+ §一.12 世界日期连续保护
2. **`engine/world.py` 新增 3 个公共函数**:
   - `get_world_last_date(world_id)`:读 state.json 的 date
   - `parse_world_date(date_str)`:兼容 2026-04-21 / 2026年4月21日 /
     20260421 三种格式
   - `validate_start_date(world_id, date_str)`:
     - 未开始:任意日期 / None(默认)
     - 已开始:date 为空 → 自动续跑(上次+1);date==上次+1 → 允许;
       否则 ValueError("只能继续模拟…想重新开始请用新的世界 ID")
3. **接入三个入口**:
   - population_runner.main:日期规范化后调用,打印"时间线校验通过"
   - simulate.py:替换"留空使用今天"为"留空=自动续跑"(交互提示
     同步改,不再默认今天——避免覆盖已开始世界)
   - server.build_sim_args:date 传入时先校验(世界不存在跳过)
4. 前端:开启模拟面板日期输入 placeholder/title 提示
   "已开始的世界不可换日期"
5. 测试:TestTimelineGuard 5 项(未开始任意/已开始精确+1/
   回退拒绝/跳日拒绝/格式解析)→ 144 项全绿

## 结果

- `test_offline.py`:144 项 OK(skipped=5)

## 偏差

- simulate.py 交互模式默认日期从"今天"改为"自动续跑"
  (遵循时间线约束的必然调整)

## 下一步候选(计划 59)

1. **前端开启模拟体验完善**(用户 2026-08 指令):
   启动后自动跟踪任务状态,完成后自动刷新结果;
   任务面板加"停止"按钮(POST /api/jobs/stop/<id>)
