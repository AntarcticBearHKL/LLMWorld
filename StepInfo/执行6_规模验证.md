# 执行 6 —— 10 户规模验证（平滑效应 + 并发实测）

> 对应 `计划6_规模验证.md`。

## 实际做了什么
1. SubAgent 并发峰值监控：`get_concurrency_stats()` 返回 (当前, 峰值)，请求进出时计数
2. population.py 构建 pop02（10 户 4 类，seed=7）
3. population_runner 末尾打印并发实测

## 验证结果（pop02 10 户 × 1 天，首轮运行）

### 平滑效应确认
| 指标 | pop01 (4户) | pop02 (10户) | 方向 |
|---|---|---|---|
| 峰均比 | 4.83 | **2.52** | ↓ 平滑生效 |
| 晚峰时刻 | 17:00 | 17:00 | 稳定 |
| 相关性 | 0.62 | 0.35 | ↓（异常户污染） |

### 并发硬约束实测
- 历史峰值并发 **10**（硬上限 10）→ **符合约束 ✓**（10 户并行下守住上限）
- 但 runner 打印处报 `MAX_WORKERS` 属性引用错误（模块常量非类属性）→ 已修

### 发现严重质量问题：决策真实性
- house_0010 达 **107 kWh**（户中位仅 12）：LLM 让 7kW 电动汽车连续 `charge_home` 超 12 小时（≈87 kWh/天）
- 户均 22.3 vs 中位 12.1、std 29.6 → 分布被异常户严重扭曲
- 相关性 0.62→0.35 的倒退即异常户晨间充电尖峰（14kW@8:15）污染所致
→ 直接触发计划7

## 成本记录
- 10 户 × 1 天：799,580 tokens（prompt 243,672 + 输出 555,908），户均/天 ≈ 80k

## 下一步
见计划7（决策真实性约束）——已执行。
