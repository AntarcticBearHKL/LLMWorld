# 执行110:社区公告注入(轻量社交网络入口)

## 实际做了什么

1. **论文依据**:Ayres et al. 2013(邻居/社区信息)+
   CoRenew 2026(共享情境)+ Fidone 2026(信息耦合)
2. **`prompts/simulate_step1_macro_plan.md`**:加 `{community_notice}`
   占位符段(空时无输出)
3. **`engine/planner.py`**:`generate_plans(time_obj, community_notice="")`
   → prompt.load 传参
4. **`engine/world.py`**:`simulate_day(..., community_notice="")`
   → planner 传递
5. **`population_runner.py`**:`--community-notice` 参数 →
   simulate_day 传递
6. **测试**:test_prompt_community_notice(注入渲染/空不泄漏)
   → **203 项全绿**

## 结果

- `test_offline.py`:203 项 OK(skipped=5)
- LLM 模拟第一条"家庭间共享信息"通道完成
  (peer-nudge=数据反馈,公告板=社区文本)

## 偏差

- 无

## 下一步候选

1. **户间双向社交网络**(完整版,需用户确认方向)
2. 公告板前端注入 UI
