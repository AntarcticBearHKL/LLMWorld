# 执行77:前端新建世界(0 token 人口生成闭环)

## 实际做了什么

1. **依据**:运维经验(生成环节缺前端入口,§七.6 一键体验)
   + 计划22 程序化人口(0 LLM,12 篇论文已存 RefPaper)
2. **`server.py`**:
   - `validate_create_args(world_id, count)`:纯函数校验
     (正则 [A-Za-z0-9_-]+ / 重名 / 1-10 户世界约束)
   - `create_world()`:调 population.build_population(0 token)
   - `POST /api/worlds/create`
3. **前端**:新增"新建世界"面板(名称/户数/生成按钮)
   → 成功自动选中新世界
4. **测试**:TestSimArgs 新增 create_world_validation
   → 166 项全绿
5. 修一处 bug:isalnum 不认下划线 → 正则 fullmatch

## 结果

- `test_offline.py`:166 项 OK(skipped=5)
- API 冒烟:POST create 生成 2 户世界成功(已清理)
- 完整闭环:新建世界(0 token)→ 开启模拟(后台)→ 自动跟踪
  → 一键分析(0 token)→ 15+ 面板

## 偏差

- PowerShell 客户端 400 误导排查;实为 server 旧进程未重启

## 下一步候选(计划 78)

1. **存档同步**(里程碑 76-77 入档)
2. **户间双向社交网络**:引擎级大功能,需用户确认方向
