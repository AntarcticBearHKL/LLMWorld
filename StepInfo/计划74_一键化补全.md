# 计划74:一键化覆盖补全 + Design 文档补全第三轮

## 做什么

1. `make_analysis_all.py` 一键化纳入 3 个新工具:
   analyze_anomalies(异常户)/ analyze_behavior_load(行为-负荷
   一致性)/ analyze_solar(光伏自用),每场景最新日执行
2. Design/.aas 补全:AnalyzeSolar/电池、AnalyzeBehaviorLoad、
   AnalyzeAnomalies、下载脚本,更新 AnalyzeTools.aas

## 为什么(依据)

1. **运维经验(§一.21)**:计划46 的一键化停在 8 个工具,
   62/64/71-72 新增 3 个工具后"一键分析"不再覆盖全部,
   用户浏览器一键后仍需手工补跑。
2. **§七.5 Design 补全**(存档契约):新模块缺 .aas。

## 怎么做

1. `make_analysis_all.py`:run_all 每场景追加
   - analyze_anomalies(复用 scan_house_profiles+build_report,
     最新日,写 anomalies_<scenario>.json)
   - analyze_behavior_load(scan_world+build_report,
     写 behavior_load_<scenario>_<date>.json)
   - analyze_solar(build_report 5kW,写 solar_<scenario>_<date>.json)
   - 汇总打印
2. Design 新增:AnalyzeSolar.aas / AnalyzeBehaviorLoad.aas /
   AnalyzeAnomalies.aas / RefPapers.aas(下载脚本);
   更新 AnalyzeTools.aas
3. 测试:TestAnomalies/TestBehaviorLoad/TestSolar 已覆盖
   各 build;make_analysis_all 冒烟(合成世界全链路)

## 怎么验证

- `python Implement/test_offline.py` 全绿
- 合成世界 make_analysis_all 冒烟(3 新 JSON 产物生成)
