# 执行44:政策组合注入(--policy "tou,nudge")

## 实际做了什么

1. **论文搜索(第四轮持续开发)**:
   - arXiv search UI:"policy mix demand response behavioral
     interventions"、"combining price information energy conservation
     households experiment" 均 0 结果 → 转向权威期刊文献
     (Faruqui & Sergici 2010;Allcott & Rogers 2014)+ LLM-ABM
     平台先例 CoRenew 2026(第一轮已搜到)
2. **`engine/policy.py`**:
   - `Policy.combine(names)`:逗号拆分为子政策,type="tou+nudge"
   - `render()`:type 含 "+" 时拼接子政策文本,
     总标题"## 当前政策组合(tou + nudge)"
   - `from_name`:含逗号自动走 combine(兼容空格 "tou, nudge");
     未知政策在组合中照样抛错
3. **`population_runner.py`**:--policy 值含逗号时场景名
   `tou+nudge`(输出目录隔离,避免逗号进路径);
   policy_schedule 自动兼容组合(走 from_name)
4. 无需改 compare_policies(自动枚举 policy 字段,矩阵自动显示组合行)
5. 测试:TestPolicy 新增 4 项(组合渲染/三政策组合/未知政策报错/
   空白容错)→ 108 项全绿

## 结果

- `test_offline.py`:108 项 OK(skipped=5)
- 人工检查:`Policy.from_name("tou,nudge").render()` 正确输出
  "政策组合(tou + nudge)"+ 分时电价 + 社会规范两段文本
- README 更新组合用法示例

## 偏差

- 无

## 下一步候选(计划 45)

1. **变异性 × 政策响应交叉验证**(Zhou 2016):
   analyze_groups 扩展,标签源接入 variability JSON
   (高/低变异性组的 TOU/nudge 响应对比)
2. **peak_demand 决策校验**:检测同小时大功率叠加,警告入 warnings
3. **工具链一键化**:make_analysis_all.py(聚类/变异性/模式/归因/分组
   一次跑完)
4. **前端**:政策组合下拉选择(前端上帝控制台侧)
