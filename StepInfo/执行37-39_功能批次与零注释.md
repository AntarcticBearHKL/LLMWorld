# 执行 37-39 —— 人口LLM混合 + 零注释标准 + 新功能批次

> 依据：`StepInfo/00_需求与规则.md`（已重读）。

## 执行37：人口生成叠加 LLM（计划37）
- 实现 `Implement/population_llm.py`：
  - 程序化基线（复用 population._build_template：类型配额/成员数/年龄关联/Big Five/电器，确定性）
  - LLM 差异化重写（每户 1 次调用）：多元文化姓名/职业/作息/爱好/性格行为描述/家庭故事
  - 校验合并：成员数与年龄以基线为准；LLM 输出非法/成员数不符 → 逐字段回退基线（不崩溃）
  - 防重复：prompt 含"社区已有家庭摘要"（最近 8 户的名字/职业/爱好）
- 新 prompt 模板 `prompts/generate_step4_household_details.md`（census 语境 + 蓝图 + 防重复指令 + 明确"基于给定 Big Five 写行为描述，不提省电"）
- CLI：`python Implement/population_llm.py pop06 --count 3 --seed 7`
- 测试（mock LLM）：合并正确性/成员数不符回退/LLM 错误回退/社区上下文 → 4 项
- 真实 LLM 生成待用户确认后执行（§一.16）

## 执行38：政策时间表 + 季节自动推断 + 新闻模板库
- **政策时间表**：`--policy-schedule "开始,结束,政策名"`（可多次，结束留空=永久）；`_policy_for_date` 每日按日期区间匹配（含端点）；与 --policy 互斥；聚合记录 day_policy；场景默认名 schedule
- **季节自动推断**：`utils.season_for_date`（南半球：12-2 夏/3-5 秋/6-8 冬/9-11 春）；runner 中 household.season 优先，否则按日期推断
- **新闻模板库** `engine/news_templates.py`：10 类模板（heatwave/cold_snap/storm/price_hike/energy_crisis/ac_tax/rebate/blackout_risk/solar_incentive/lockdown）；`--event-template "日期|模板名"` 注入
- 测试：政策区间 3 项 + 季节 5 项 + 模板 3 项 → 78 项 OK（5 数据依赖跳过）

## 执行39：代码零注释标准（用户指令）+ 数据全清空
- **零注释清理**：tokenize 安全脚本删除全部 # 注释与 docstring（空块补 pass）；utf-8-sig 读取顺带清除 BOM（发现 6 个文件带早期 PowerShell 写入的 BOM）；前端 HTML/JS 注释正则清理（API 路径完好验证）
- 调试历程（重要）：v1/v2 脚本用 `src.rfind("\n", 0, col)` 误把列号当文件偏移 → 行前缀判定全错 → 修复为按行号取前缀；docstring 删除导致空块 → 块唯一 docstring 替换为 pass
- 数据全清空：停止全部后台任务、删除 worlds/ 与 outputs/ 全部内容、删除模拟报告（用户指令：手工模拟模式）
- 数据依赖测试改 skipUnless（数据缺失自动跳过）
- 存档：§一.16 模拟触发规则 / §一.17 人口生成叠加 LLM / §一.18 代码零注释 / §一.19 手工模拟模式

## 当前状态
- 78 项离线测试 OK（5 项数据依赖跳过）
- 代码库全部零注释；服务器常驻；无模拟数据（用户手工模拟）
- 所有提交：git log 可见（HEAD ~ step39）
