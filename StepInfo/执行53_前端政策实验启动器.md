# 执行53:前端政策实验启动器

## 实际做了什么

1. **论文依据**:CoRenew (2026) arXiv:2607.25447 政策评估 UI
   (LLM-ABM 平台标准能力)+ 运维经验
2. **`server.py`**:
   - `build_sim_args(world_id, body)`:policy/days/date/scenario →
     population_runner 参数列表;policy 先 Policy.from_name 校验
   - `POST /api/worlds/<id>/simulate`:构造参数 →
     background_runner.cmd_start → 返回 job_id+command
3. **前端**:新增"政策实验启动"面板——政策下拉
   (6 政策+2 组合)、天数/日期输入、启动按钮
   (confirm 对话框声明"将消耗 API tokens",§一.16 合规)
4. 任务面板已有,进度自动监控
5. 测试:TestSimArgs 5 项(基线/政策+日期/组合/非法政策/场景覆盖)
   → 130 项全绿

## 结果

- `test_offline.py`:130 项 OK(skipped=5)
- server 重启完成,新端点就绪(未真启动模拟,§一.16)

## 偏差

- 无

## 下一步候选(计划 54)

1. **LLM 模拟管线**:家庭间社交网络/个性化 nudge
   (邻居真实用电对比文本,同伴效应)——引擎级,需用户确认
2. **模式迁移 × 事件联动**:迁移次数与新闻/政策敏感度交叉
3. **前端**:天气卡片/世界时间线面板(00 规则 §七.2 遗留)
