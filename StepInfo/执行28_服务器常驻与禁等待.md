# 执行 28 —— 服务器系统级常驻 + 前端任务监控（禁等待规则落地）

> 依据：`StepInfo/00_需求与规则.md` §一.14/§一.15（已重读）。

## 用户指令（本轮新增）
1. 只要开始开发，随时**系统级开启服务器**；每次前后端修改后**重启服务器**
2. **任何时候不允许等待进程**（尤其 Start-Sleep）；所有等待行为转系统级后台进程；不允许任何任务阻碍主进程；即使"必须等测试结果"也改为优化当前代码/项目结构

## 实际做了什么
1. **background_runner 增加 server 管理子命令**：`server start|restart|stop|status [--port]`
   - PID 存 `logs/jobs/server.pid`，输出 `logs/jobs/server.out`（系统级常驻，不占会话）
   - 前后端修改后一键 `server restart`
2. **前端后台任务监控面板**：`/api/jobs`（复用 background_runner 的 parse_progress/tail_text）+ 前端"后台模拟任务"面板（状态着色/进度/命令/日志尾部），10 秒自动刷新
3. **server.py 优化**：background_runner 模块级导入（避免每请求重复 sys.path/import）
4. **存档更新**：§一.14 服务器常驻、§一.15 禁等待（硬性）；里程碑表 +28 行
5. 服务器当前状态：PID 12256 @ 8080（已按规则在每次前后端修改后 restart）

## 状态
- job_004（pop04 去作弊化 baseline）后台运行中（约 5 分钟，无并发限制下比 pop03 快得多）
- 63 项离线测试全绿

## 教训归档
- 宽泛进程匹配（`Get-Process python | Stop-Process`）曾误杀 job_003 → 已入 §一.15 规则；清理必须精确 PID

## 下一步候选
- pop04 完成后：pop03 vs pop04 去作弊化对比（用电水平/形状/异质性）
- pop04 政策/新闻实验
- 续跑实战：pop04 第 1 天完成后不指定日期再启动 → 第 2 天继续
