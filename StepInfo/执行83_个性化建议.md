# 执行83:个性化节能建议引擎(advisor)

## 实际做了什么

1. **论文搜索**:arXiv search UI 搜 "residential demand flexibility
   potential assessment" → Monacchi et al. (2015) arXiv:1505.01311
   (Mjölnir advisor)→ PDF 已下载 RefPaper(30/30)
2. **新增 `Implement/analyze_advice.py`**:
   - `advice_for(metrics)`:规则引擎(高耗能 z>2 / 叠加>0 /
     晚峰 17-21 / 峰均比>5 → 对应建议文本)
   - `build_report`:复用 analyze_anomalies + 每户指标 →
     建议列表(健康户"保持习惯")
   - 输出 advice_<scenario>_<date>.json
3. **`server.py`**:`GET /api/worlds/<id>/advice/<scenario>/<date>`
4. **前端**:新增"个性化节能建议"面板(每户建议卡片)
5. **测试**:TestAdvice 3 项 → 175 项全绿

## 结果

- `test_offline.py`:175 项 OK(skipped=5)
- RefPaper 30/30
- server 重启完成

## 偏差

- "健康户"测试峰时设计为午后(晚峰建议对所有晚峰家庭适用,
  属信息类建议非问题类)

## 下一步候选(计划 84)

1. **存档同步**(里程碑 82-83 入档 + Design)
2. **户间双向社交网络**:引擎级大功能,需用户确认方向
