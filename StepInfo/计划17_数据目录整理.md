# 计划 17 —— 数据目录整理（根目录数据 → Data/）

> 依据：`StepInfo/00_需求与规则.md`（已重读，新增"五、前端与数据组织"节）。

## 为什么做这步

根目录堆积了数据文件与脚本（census CSV、电力 CSV、翻译缓存、整合脚本），与代码混杂。按用户要求统一放入 `Data/`，并同步更新代码引用。

## 做什么

| # | 任务 | 验收 |
|---|---|---|
| 1 | 移动数据文件到 Data/：clayton_3168_*.csv ×5、vic_electricity_data.csv、clayton_translation_cache.json | 根目录清净 |
| 2 | 移动数据相关脚本到 Data/tools/：integrate_census.py、test_openelectricity.py | 脚本随数据 |
| 3 | 更新代码引用：validate_baseline.py 的默认真实数据路径 → Data/vic_electricity_data.csv | 脚本可跑 |
| 4 | 检查全项目还有无其他根目录数据引用（grep csv/json 路径） | 无遗漏 |
| 5 | 跑测试确认无回归 + git 提交 | 全绿 |

## 注意事项
- Data/ 已在 .gitignore（数据不入库）
- 脚本内硬编码旧路径（integrate_census.py 里 C:/Users/antar/Desktop/LLMWorld/...）一并修正为相对路径
- 不删除任何历史数据，只移动

## 验证
1. `python Implement/test_offline.py` 全绿
2. `python Implement/validate_baseline.py --world pop02` 可跑（真实数据从新路径加载）
3. git 提交
