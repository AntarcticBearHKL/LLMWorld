# LLMWorld Research Console - backend dependencies
# Install into the existing research venv:
#   .venv\Scripts\python.exe -m pip install -r dashboard\requirements.txt
# (uv is also fine: uv pip install -r dashboard\requirements.txt)
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.1
orjson>=3.10
