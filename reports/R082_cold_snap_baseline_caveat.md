# R082 — 冷事件扩样受阻：world_143345 基线已含采暖（基线非普适为0）

- **轮次**：Round 82（**0 token**）
- **日期**：2026-09-11
- **结论**：⚠️ **重要 validity 提醒**：`world_143345` house_0001 的**基线已含采暖**
  （M2 3.0、M5 1.5 kWh）→ 不是冷事件的"干净基线"，cold_snap 的"baseline 0/8"结论**具有世界特异性**

---

## 1. 目标

欲用 `world_143345` house_0001（6 人、4 台 SpaceHeater）扩样 cold_snap 以达**双侧**显著
（当前 0/8 vs 4/8，双侧 p=0.077）。**先核对基线**（0 token）。

## 2. 结果（world_143345 house_0001，2026-09-11 基线）

| 成员 | 基线采暖 kWh |
|---|---|
| Member 1 | 0.000 |
| **Member 2** | **3.000** |
| Member 3 | 0.000 |
| Member 4 | 0.000 |
| **Member 5** | **1.500** |
| Member 6 | 0.000 |

- 采暖设备：`bedroom_2_spaceheater`、`bedroom_5_spaceheater`；
- **基线下 M2/M5 已采暖（非 0）**——该住户**不适合**作为"采暖启用"的二值样本。

## 3. 解读（validity）

- **基线非普适为 0**：此前 cold_snap 的"baseline 0/8"成立，是因为 `world_838587`/`world_172148`
  的基线**恰好**未采暖；`world_143345` 的基线**已经采暖**（M2/M5）。
- **含义**：cold_snap 的"baseline 0 → 事件启用"是**世界/时段特异**的；
  不能断言"所有世界基线都不采暖"。扩展样本须核对基线，或改用**配对差值**（同户 baseline vs 事件）。
- **不扩样**：因基线被污染，本轮**不**将 world_143345 house_0001 加入 cold_snap 汇总；
  cold_snap 维持 **0/8 vs 4/8（双侧 0.077，未显著）**。

## 4. 更广的主线印证

- "**基线随世界/日期变化**"再次出现（如同 R057 的噪声地板、R080 的内在谷期）——
  在 LLM 仿真中，**"对照"必须逐案例核对**，不能假设某个默认值普适。

## 5. token 消耗

- **0**（仅读取既有产物）。

## 6. 结论

cold_snap 扩样受阻（基线污染）；其"baseline 0"结论为**世界特异**。
已如实记录；cold_snap 维持单侧显著（0.038；双侧 0.077）。
