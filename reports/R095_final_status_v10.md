# R095 — 最终状态 v10（含 B1/B3 新结论）

- **轮次**：Round 95（检查点；**0 token**）
- **日期**：2026-09-11
- **用途**：最新总览；承接 `R087_final_status_v9` 与 `goal.md §2`。

---

## 1. 仓库与测试
- 提交：R1–R094（`step111`–`step207`），全部 push；HEAD `4dc62ff`。
- 测试：`tests/` **270/270 全绿**；源码树干净；`output/` 已**入库上传**（用户指示）。
- 能力：guide 全部干预入口 + `--policy-schedule`/`--peer-nudge`/`--natural-ev`/采样控制/
  `compare_worlds.py`/`aggregate_runs.py`；漂移清零。世界：`world_838587`、`world_172148`、`world_143345`。

## 2. 三/五个显著结论

| 结论 | 证据 | 显著性 | 复现 |
|---|---|---|---|
| **热浪 → 空调启用** | baseline AC **0/9** vs heatwave **11/12** | 双侧 **p≈0.00003** | 跨 3 世界 |
| **封锁 → 外出归零** | baseline Out>0 **13/13** vs lockdown **1/13** | 双侧 **p≈0.000003** | 跨 3 世界 |
| **封锁 → 日间负荷** | **+2.183 kWh（+122%）**，13/13，t≈8.04 | 可判幅度 | 跨 3 世界 |
| **寒潮 → 采暖启用** | baseline **0/10** vs cold_snap **6/10** | 双侧 **p≈0.011** | 跨 3 世界 |
| **停电预警 → 峰段削减** | N=3：4.686→3.203 kWh | **−31.7%**（z≈−9.6） | 单世界（待复现） |
| **电价上涨 → 峰段削减** | N=3：4.686→3.849 kWh | **−17.9%**（z≈−5.4） | 单世界（待复现） |

## 3. 否定 / 不可定论
TOU（采样敏感）、nudge（方向不稳健）、nudge_loss（反例 +6%）、peer-nudge（不一致）、
RQ3 分组（方差压倒）、storm（null）、EV 峰移（**内在谷期天花板**）、`peak_demand`（N=3 null）。

## 4. 方法学产出
1. **噪声地板**：总电量 std≈1.0 kWh（CV≈12%，R057）；**峰段(16–21) kWh 是低噪指标**（CV≈3.3%，R094）。
2. **功效分析**：文献尺度幅度（~3%）需 ~124 run/臂 → 不可行；仅大效应可判（R058）。
3. **事件类型边界（修正版）**：提供 **timing 动机** 的事件有效（热浪/寒潮/封锁/价格/停电预警）；
   纯信息性（storm）无效。
4. **基线世界特异**（R082）→ 须配对设计；**措辞主导**（R040）。

## 5. 本轮新增（用户指示 A/B/C）
- A1 output 上传；A2 文档漂移（guide）；A3 论文提级；goal.md v2。
- B1 寒潮扩样→双侧显著；B2 peak_demand→null；B3 停电预警/电价上涨→显著削峰。
- C1 已在 B2/B3 中示范（N=3 mean±std）；C2 EV prompt→**moot**（内在天花板，不改）。

## 6. 下一步（写入 goal.md）
- **C1 续**：对 `blackout_risk` / `price_hike` 做**跨世界/成员**复现（升级为强结论）。
- 用**峰段低噪指标**评估更多中等效应干预（TOU、nudge 等）——比总电量更易检出。
- 其余 B（占位实验 night_setback/in_home_display/policy_tradeoffs）。
- **不做**：RQ4、RQ1。
