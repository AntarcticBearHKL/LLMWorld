# R087 — 终稿状态 v9（跨 3 世界的两个强显著结论）

- **轮次**：Round 87（检查点；**0 token**）
- **日期**：2026-09-11
- **用途**：最终状态；细节见 R081（handoff）、R085/R086（3 世界扩样）。

---

## 1. 仓库状态

- **提交**：R1–R86 共 **86** 个 step 提交，全部 push 到 `origin/main`（最新 `318d078`）。
- **测试**：`tests/` **270/270 全绿**；`compileall` 干净；源码树干净（仅 `output/` 未跟踪）。
- **能力**：guide 全部干预 + news/community（天气联动）+ `--policy-schedule` + `--peer-nudge` +
  `--natural-ev` + 采样控制 + `compare_worlds.py` + `aggregate_runs.py`；漂移清零。
- **世界**：`world_838587`、`world_172148`、`world_143345`。

## 2. 两个跨 3 世界的强显著结论（核心）

| 结论 | 证据（3 世界） | 双侧 p |
|---|---|---|
| **热浪 → 空调启用** | baseline AC **0/9** vs heatwave **11/12** | **≈0.00003** |
| **封锁 → 外出归零** | baseline Out>0 **13/13** vs lockdown **1/13** | **≈0.000003** |

两者均为**大效应二值**信号（设备 on/off、外出有/无），跨 3 个独立世界复现，并有个体异质性。

## 3. 阴性/弱结论

- **寒潮 → 采暖**：单侧 p=0.038、**双侧 0.077（不显著）**；且在 world_143345 **未复现**（配对 null，R084）。
- **storm**：null（R074）→ **事件类型边界**：仅改变行为结构的事件可判。
- **TOU / nudge / nudge_loss / RQ3 分组**：均不可定论（采样敏感、方向不稳、方差压倒）。
- **EV 峰移**：**内在天花板**（基线及 `--natural-ev` 均 100% 谷期，R076/R077/R080）→ 不可评估。

## 4. 方法学产出

1. **噪声地板**：同配置 3 次总电量 std 1.01 kWh（**CV≈12%**），极差 +33%（R057）。
2. **单次/单臂不可信**（R024/R026/R045→R046/R048→R054）。
3. **功效分析**：文献尺度效应（~3%）需 ~124 run/臂（不可行）；仅大效应（≥10–20%）可判（R058）。
4. **基线世界特异**：事件效应条件于基线设备态；须配对设计（R082/R083）。

## 5. 论文状态

`00_intro`（两个强显著事件）/ `01_method` / `99_discussion`（Threats-to-validity：过度遵从、框架不稳定、
噪声地板、单次不可信、事件类型边界、基线世界特异）；`experiments/` 14 文件。

## 6. 需用户决策 / 后续

1. **幅度结论**：是否投入大规模多种子平均（R058 估 10–20% 效应需 ~3–11 run/臂；文献尺度不宜）。
2. **EV 实验**：是否改 s4 默认 prompt（当前默认即谷期，EV 干预失效）。
3. 更多大效应二值事件（低成本、可显著）。

## 7. 复现入口

```powershell
.venv\Scripts\python.exe -m unittest discover -s tests -v      # 270 全绿
python run.py --mode simulate --world world_143345 --house house_0002 --days 1 --event-template "2026-09-11|heatwave"
```
