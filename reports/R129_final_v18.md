# R129 — 最终状态 v18（R104–R128）

- **轮次**：Round 129（检查点；**0 token**）
- **日期**：2026-09-11
- **用途**：取代 R127/v17；**修正 R126 的 2×2**。

---

## 1. 仓库与测试
- 提交：`step111`–`step264`；全部 push；HEAD `a011f7e`；与 `origin/main` 同步。
- 测试：`tests/` **270/270 全绿**；源码树干净；`output/` 已入库。
- `reports/R001–R129`。

## 2. 四个有效结论（均跨 3 世界）

| 结论 | 证据 | 显著性 |
|---|---|---|
| **热浪 → 空调** | baseline AC 0/9 vs 11/12 | 双侧 p≈0.00003 |
| **封锁 → 外出归零** | 13/13 vs 1/13（+ 日间 +122%） | 双侧 p≈0.000003 |
| **寒潮 → 采暖** | 0/10 vs 6/10 | 双侧 p≈0.011 |
| **空调峰税 ac_tax → 削峰** | AC-on 15/15→7/15；峰段 −22.5%/−24.6%/−20.6% | p≈0.0022；合并 p≈1e-7 |

## 3. 削峰机制：**条件于房屋"设备-时段结构"**（R118/R119/R123/R126/**R128**）

- **削峰需干预真正降低"峰段窗口"负荷**，而这是**房屋特异**的：
  - `world_838587`（**卧室 AC，夜间用**）：靶向信息/通用 `rebate` **降耗不削峰**；仅靶向 `ac_tax`（价格）削峰 −22.5%；
  - `world_172148`（**客厅 AC，晚峰负荷**）：**同一靶向信息即削峰 −25.5%（t=−4.81, 9/9）**。
- **R126 的"价格×靶向"2×2 已撤回**（仅世界特异）。
- **纯信息**（`energy_crisis`/`storm`）两世界皆**无效**。
- **节能 ≠ 削峰**：请求可大幅降 AC（−58%）却不触及峰段。

## 4. null / 撤回
- `tou`：n=15 **null**（小样本假象 n=3 −17.5%→n=9 +13.1%→n=15 −3.2%）。
- `in_home_display`/`peak_demand`/`night_setback`：same-era null（n<15）。
- `blackout_risk`/`price_hike`：**撤回**（漂移伪影）。

## 5. 方法学（三条红线 + 一条要求）
1. 连续指标跨时段**漂移** → 须 same-era；二值/结构漂移免疫（R104/R106）。
2. 连续幅度 **n<15 不可信**（R112/R114）。
3. **机制结论须跨世界检验**（R128：单世界机制不可外推）。
4. 噪声地板 CV≈12%（R057）；文献 ~3% 不可行（R058）。

## 6. 下一步
- 论文进入**收束撰写**（4 事件 + 削峰条件性机制 + 方法学红线）；`solar_incentive`/EV 低价值或阻塞；RQ4/RQ1 跳过。
