# R108 — 最终状态 v12（含 R104–R107 validity 修正）

- **轮次**：Round 108（检查点；**0 token**）
- **日期**：2026-09-11
- **用途**：R104–R107 修正后的**可信结论集**；取代 R095/R100。

---

## 1. 仓库与测试
- 提交：R1–R107（`step111`–`step230`），全部 push；HEAD `b087e63`。
- 测试：`tests/` **270/270 全绿**；源码树干净；`output/` 已入库。

## 2. 可信结论集（经 same-era 验证，漂移免疫）

| 结论 | 证据 | 显著性 | 类型 |
|---|---|---|---|
| **热浪 → 空调启用** | baseline AC **0/9** vs heatwave **11/12** | 双侧 p≈0.00003 | 二值（3 世界） |
| **封锁 → 外出归零** | baseline Out>0 **13/13** vs lockdown **1/13** | 双侧 p≈0.000003 | 二值（3 世界） |
| **封锁 → 日间负荷** | **+122%（跨成员）**；same-era **+379%** | 大效应（R107 确证） | 连续（结构，未漂移） |
| **寒潮 → 采暖启用** | baseline **0/10** vs cold_snap **6/10** | 双侧 p≈0.011 | 二值（3 世界） |

- **R106 same-era 复现**：三二值事件在 fresh 基线下均成立。

## 3. 已撤回（基线跨时段漂移伪影，R104/R105）
- `blackout_risk` / `price_hike` 及其余事件的**连续峰段"削峰"**（−7%~−32%）——纯属拿旧基线（4.686）
  比较造成；same-era 下 blackout_risk 峰段 **+15.9%（无效应）**；**中性事件**亦"削峰 −32.9%"。

## 4. 否定/不可定论（二值或 same-batch）
- TOU（采样敏感）、nudge（不稳）、nudge_loss（反例）、peer-nudge（不一致）、RQ3 分组（方差压倒）、
  storm（null）、EV 峰移（内在天花板）、`peak_demand`（same-batch null）、
  `in_home_display`/`policy_tradeoffs`（跨时段，不可靠）。

## 5. 方法学（核心红线）
1. **连续指标跨时段会系统性漂移**（旧/新同配置基线峰段差 36%）→ 跨时段比较无效；
2. **二值/结构指标漂移免疫**（设备开/关、外出/居家）→ **首选**；
3. 连续指标须 **same-era（同批次紧邻）** 对照；
4. 噪声地板：总电量总 CV≈12%（R057）；功效分析：文献尺度幅度不可行（R058）。

## 6. 下一步
- 仅对**二值事件**或 **same-era 连续对照**继续；`output` 已入库；drift 红线已写入 `goal.md`。
- RQ4/RQ1 按用户指示跳过。
