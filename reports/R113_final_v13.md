# R113 — 最终状态 v13（R104–R112 修正后）

- **轮次**：Round 113（检查点；**0 token**）
- **日期**：2026-09-11
- **用途**：R104–R112 修正后的**可信结论集**；取代 R108/v12。

---

## 1. 仓库与测试
- 提交：`step111`–`step239`；全部 push；HEAD `6cae448`；与 `origin/main` 同步。
- 测试：`tests/` **270/270 全绿**；源码树干净；`output/` 已入库。
- `paper/`：README + 00/01/99 + **16 个** `experiments/*.md`；`reports/R001–R113`。

## 2. 可信结论集（same-era / 二值验证）

| 结论 | 证据 | 显著性 | 类型 |
|---|---|---|---|
| **热浪 → 空调启用** | baseline AC **0/9** vs heatwave **11/12** | 双侧 p≈0.00003 | 二值（3 世界） |
| **封锁 → 外出归零** | baseline Out>0 **13/13** vs lockdown **1/13** | 双侧 p≈0.000003 | 二值（3 世界） |
| **封锁 → 日间负荷上升** | +122%（跨成员）；same-era **+379%**（R107） | 大效应（结构，未漂移） | 连续-结构 |
| **寒潮 → 采暖启用** | baseline **0/10** vs cold_snap **6/10** | 双侧 p≈0.011 | 二值（3 世界） |

- **R106 same-era 复现**：三二值事件在 fresh 基线下均成立 → **漂移免疫**。

## 3. 已撤回 / null（修正）
- ❌ `blackout_risk` / `price_hike` **连续削峰**（−7%~−32%）：**基线跨时段漂移伪影**（R104/R105）；
  same-era blackout_risk = **+15.9%**（无效应）。
- ❌ `in_home_display`：same-era 交错 **null**（峰段 −10.3% n.s.，总电量 +4.2%，R109）。
- ❌ `peak_demand`：same-era **null**（R092）。❌ `night_setback`：**null**（R099）。
- ❌ `tou`：**n=9 null**（峰段 −2.2% n.s.）；总电量 **+13.1%**（7/9 对，t=2.11，非漂移）borderline（R112）。
  —— n=3 曾见 −17.5%，扩样后**塌缩**（**小样本假象**）。

## 4. 方法学（核心红线）
1. **连续指标跨时段系统性漂移** → 跨时段比较无效；须 **same-era（同批次紧邻）**；
2. **二值/结构指标漂移免疫** → 首选；
3. **n=3 连续幅度不可信**（R112）：TOU n=3 −17.5% → n=9 −2.2%；连续幅度须 **n≥9~15**；
4. 噪声地板：总电量 CV≈12%（R057）；文献尺度 ~3% 不可行（R058）。

## 5. 下一步（唯一关键路径）
- 仅做 **二值事件** 或 **same-era n≥9** 的连续对照；`output` 已入库。
- RQ4/RQ1 按用户指示跳过；EV 干预因内在天花板暂停（C2）。
