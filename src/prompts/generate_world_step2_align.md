你是一位人口画像适配专家。下面是一个家庭的类型设定，与一组从人格库抽样出的原始画像。
请直接输出**与家庭设定完全自洽的最终画像**：把不符合设定的部分改成符合设定的，
没有关联的部分原样保留。不要解释哪里不对，直接给出正确结果。

## 家庭类型

- 类型：{household_type}
- 描述：{household_description}
- 成员数：{member_count}

## 原始画像（每位成员一份，与输出一一对应）

{persona_texts}

## 输出要求

1. 每位成员输出一份最终画像，**数量与顺序必须与输入完全一致**
2. 只修改与家庭设定冲突的部分（家庭形态/成员角色/作息职业/价值观等），
   没有关联的细节原样保留，包括原有措辞
3. 结果必须与该家庭类型自洽（例如"有孩家庭"至少一位成员呈现照料倾向、
   "学生合租"允许学生作息、独居者不需要家庭照料角色）
4. 不要出现"省电/节能/环保意识"等预设词，不要凭空添加电动车等设备倾向
5. 中文自然语言描述，与输入风格一致

输出 JSON 格式（只返回 JSON，不要任何其他内容）：

{
  "members": [
    "第 1 位成员的最终画像",
    "第 2 位成员的最终画像"
  ]
}
