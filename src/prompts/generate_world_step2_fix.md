你是一位人口画像审核修正专家。下面是一个家庭的类型设定与一组成员人格画像，
审核发现画像与该家庭类型存在冲突。请**只修正冲突的部分**，无关细节一律原样保留。

## 家庭类型

- 类型：{household_type}
- 描述：{household_description}
- 成员数：{member_count}

## 审核发现的不合格原因

{reasons}

## 原始画像（每位成员一份，与输出一一对应）

{persona_texts}

## 修正要求

1. **只修改与不合格原因直接相关的部分**（如：家庭形态不匹配、成员角色缺失、
   作息/职业/价值观与该家庭类型冲突等）
2. **没有关联的部分必须原样保留**，包括原有措辞与细节，不要重写整段
3. 成员数量必须与输入一致，每位成员一份，顺序对应
4. 修正后的画像要能与家庭类型自洽（例如"有孩家庭"至少一位成员呈现照料倾向、
   学生家庭允许学生作息等）
5. 画像中不要出现"省电/节能/环保意识"等预设词，不要凭空添加电动车等设备倾向
6. 仍用中文自然语言描述，保持与输入相同风格

输出 JSON 格式（只返回 JSON，不要任何其他内容）：

{
  "members": [
    "修正后的第 1 位成员完整画像",
    "修正后的第 2 位成员完整画像"
  ]
}
